# Worklog — 2026-07

## E1-E3 fixes + benchmark (2026-07-02, branch `fix/ppo-e1-e3`)

Implemented the top-3 items from `docs/PPO-REVIEW-FB.md` (commit `d60da7f`;
implementation by a subagent, reviewed line-by-line):

- **E1** `TokenSetExtractor` default `dropout` 0.1 -> 0.0 (SB3 collects
  rollouts in eval mode but runs `evaluate_actions` in train mode, so
  dropout>0 contaminates the PPO ratio).
- **E2** token-v1 `op_reachable`/`exposed_to_lethal` now sum the WHOLE
  opponent board (`start_turn` refreshes every creature; readiness-filtering
  excluded exactly last turn's attackers).
- **E3** eval-callback bucket-crossing gating; per-env seed stride (50k);
  `BattleEnv.reset` reseeds the opponent. One regression test per change;
  470 passed.

**Benchmark** (B0 recipe: token V0, lr=1e-4, target_kl=0.025, 800k zoo
curriculum, n_envs=16; 3 seeds/arm; verdicts via `locma ceiling-eval`
--seeds 40 --games-per-seed 25, paired vs `runs/b0_s{0,1,2}.zip`):

| arm | vs | delta | 95% CI | verdict |
|---|---|---:|---|---|
| `e1_s*` dropout-0, token V0 | B0 | **-0.037** | [-0.044, -0.030] | regression |
| `e2_s*` dropout-0, token V1-fixed | e1 arm | **+0.008** | [+0.002, +0.015] | real but sub-threshold |

(cand=0.620 / B0=0.657 for E1; cand=0.629 / base=0.620 for E2. Training was
~11 min/run this session vs R1's ~24 — unexplained, possibly KL-early-stop
frequency shifting without dropout noise; diagnostics in
`runs/e1e3/train_*.log`.)

**Honest read of E1: removing dropout at the tuned recipe made the model
WORSE, clearly (CI entirely negative).** The mechanistic claim stands — the
ratio contamination is real — but its net effect at the *gentle* B0 recipe
(lr=1e-4 + KL cap) was evidently beneficial regularization, not just noise.
Two confounds noted: (a) the e1 arm bundles the E3 seed-stride hygiene (b0
models predate it); (b) without dropout noise `approx_kl` reads lower, so
the `target_kl=0.025` early-stop fires later -> effectively MORE update
epochs per batch -> a more aggressive optimization regime than the recipe
was tuned for. The overnight battery below discriminates.

**E2 read:** the corrected V1 threat scalars give a small, CI-clean lift
(+0.008) over V0 at matched recipe/dropout - the first obs-side change to
produce a nonzero paired delta, but far under the +0.03 headroom bar. Worth
re-testing at whatever recipe wins the dropout question.

**Overnight battery (N-arms, 3 seeds each, all evaluated paired vs B0):**

- **N0** dropout=0.1 explicit + current code (isolates the E3 stride
  confound: if N0 ~= B0, the -0.037 is all dropout).
- **N1** lr=3e-4, target_kl=None, dropout=0 (the config the worklog
  declared broken - retested without the alleged confounder).
- **N2** lr=3e-4, target_kl=0.025, dropout=0 (separates LR from KL cap).

Decision rule: if N0 ~= B0 and N1/N2 < B0, dropout was a *feature* at this
scale - revert the E1 default to 0.1 (keeping the mechanism documented) and
the ceiling verdict hardens. If N1 or N2 >= B0, the "gentle PPO" recipe was
compensating for dropout and the HP story reopens.

## N-battery results + dropout revert (2026-07-02, early morning)

All three arms trained and evaluated clean (paired vs `runs/b0_s{0,1,2}.zip`,
40 seeds x 25 games):

| arm | config | delta vs B0 | 95% CI |
|---|---|---:|---|
| N0 | dropout=0.1 explicit, current code | -0.009 | [-0.016, -0.002] |
| N1 | lr=3e-4, target_kl=None, dropout=0 | **-0.247** | [-0.256, -0.239] |
| N2 | lr=3e-4, target_kl=0.025, dropout=0 | -0.022 | [-0.029, -0.015] |

**Decomposition of E1's -0.037:** N0 shows the bundled E3 hygiene costs at
most ~-0.01 (tiny; possibly seed noise around the stride change), so
**dropout removal accounts for ~-0.028** (N0 0.648 vs e1-arm 0.620 at
identical code). **N1 refutes the "recipe was compensating for dropout"
hypothesis outright**: lr=3e-4 without a KL cap collapses to 0.410 even with
dropout OFF -- the token net's high-LR instability is intrinsic, not
dropout-borne. N2 confirms the KL cap (not low LR alone) does most of the
rescuing (0.635, within ~0.02 of B0).

**Decision (per the pre-committed rule): dropout was a feature.** Reverted
the TokenSetExtractor default to 0.1; the constructor comment + a pinning
test now document both the PPO ratio-noise caveat and the measured
regularization win, so this doesn't get "fixed" again without a paired eval.
The E2 v1-scalar fix and all E3 hygiene fixes stand (correctness, ~free).

**Ceiling verdict hardened:** B0 (token V0, lr=1e-4, target_kl=0.025,
dropout=0.1) = **0.657** remains the best reactive recipe. HP tuning (R2/R4),
dropout removal, and higher-LR regimes all regress or tie. Review-doc H4
("optimization pathology is self-inflicted") is now largely refuted on the
dropout front; H1/H2 (planning gap) stand untouched.

**Next (launched):** R5-proper -- token-v1 (fixed scalars) at the winning
recipe (dropout 0.1) x3 seeds, paired vs B0. The earlier +0.008 v1-vs-v0
delta was measured on the handicapped dropout-0 arms; this is the design
doc's Phase-2 verdict on the correct ruler.

## R5-proper: token-v1 (fixed) at the winning recipe -- VERDICT #2 (2026-07-02)

`r5_s{0,1,2}` = token-v1 (corrected threat scalars), B0 recipe (lr=1e-4,
target_kl=0.025, dropout=0.1), 800k zoo curriculum, paired vs B0:

```
cand=0.660  B0=0.657  delta=+0.003  95% CI [-0.004, +0.010]
VERDICT: ceiling-confirmed
```

**VERDICT #2 (Phase 2, obs-encoding): null.** The +0.008 v1-vs-v0 edge
measured on the dropout-0 arms did not carry to the winning recipe -- with
dropout restored, v1-fixed is statistically indistinguishable from V0
(CI straddles zero, nowhere near +0.03). The symmetric-threat scalars are
neutral: the regularized net either already infers that information from
the tokens or cannot exploit it reactively.

### Night summary -- full verdict table (all paired vs B0=0.657, 40x25)

| arm | change vs B0 | delta | 95% CI | read |
|---|---|---:|---|---|
| e1 | dropout 0, V0 | -0.037 | [-0.044, -0.030] | dropout removal hurts |
| e2 | dropout 0, V1-fixed | (+0.008 vs e1) | [+0.002, +0.015] | small, arm-local |
| n0 | dropout 0.1, E3 hygiene only | -0.009 | [-0.016, -0.002] | hygiene ~free |
| n1 | lr 3e-4 uncapped, dropout 0 | -0.247 | [-0.256, -0.239] | high LR intrinsic collapse |
| n2 | lr 3e-4 + KL cap, dropout 0 | -0.022 | [-0.029, -0.015] | cap does the rescuing |
| r5 | V1-fixed, winning recipe | +0.003 | [-0.004, +0.010] | **Phase-2 null** |

**Bottom line for the ceiling study: both design-doc verdicts are now in and
both confirm the ceiling** -- #1 HP tuning (R4, -0.040) and #2 obs encoding
(R5, +0.003). Everything training-side is spent; B0 (token V0, lr=1e-4,
target_kl=0.025, dropout=0.1) at **0.657** is the reactive recipe of record.
The open levers are the planning-side ones from `docs/PPO-REVIEW-FB.md`:
E3a (tactics diagnostic suite), E4 (netdmcts distillation with soft targets
+ DAgger), E5 (learned-value own-turn planner). Code kept from this branch:
the E2 v1-scalar correctness fix, all E3 hygiene fixes, the dropout revert
with its pinning test.

## Post-study cleanup: sweep + puffer machinery removed (2026-07-02, branch feat/ppo-ceiling-study)

With both ceiling-study verdicts in (HP tuning null, obs encoding null), the
study-only machinery earns its exit. Removed, recoverable from git history:

- **Optuna sweep**: `locma/envs/sweep.py`, the `locma sweep` CLI command, the
  `trial=`/pruning plumbing in `WinRateEvalCallback`, the 4 `test_sweep_*.py`
  files + 2 trial-path callback tests, and the `[sweep]` extra (optuna,
  tensorboard) from pyproject/uv.lock. A re-sweep was already ruled out by
  design (R4 verdict); anyone reviving it should also re-read finding 8 in
  `docs/PPO-REVIEW-FB.md` (infeasible-config handling, pruner choice).
- **PufferLib Gate-0 spike**: `scripts/puffer_bench.py` + test. PufferLib has
  no native Windows support (worklog Gate-0 entry has the 4-release evidence
  and the SPS table); the study ran on sb3 and the recipe of record is sb3.

Kept: `WinRateEvalCallback` itself (in-training telemetry, bucket-crossing
fix), `ceiling-eval` CLI + paired-bootstrap harness (the ruler for all future
experiments), the full HP plumbing on `train_zoo` (generic, not sweep-bound),
and the token-v1 obs variant (null result but a correct, tested variant; the
E2 threat-scalar fix lives there). Post-cleanup: ruff clean, 461 passed,
1 skipped.

Archival docs (`ppo-ceiling-study-{design,plan,HANDOFF}.md`) still reference
the removed tooling by design -- they document the study as it ran.

## Draft-noise study: partial random draft (2026-07-02)

New lever, orthogonal to the (confirmed-null) training-side HPs: **deck
diversity**. `PartialRandomDraftPolicy` wraps a base draft and replaces exactly
`k` of the 30 picks with uniform-random ones (keyed on draft round, so the
battle env's draft-both-seats gives each deck exactly `k` random picks;
`note_pick` keeps the stateful `balanced` tracker accurate). Exposed as
`+rndK` draft-bench specs, `--draft-noise K` on `train`/`train-zoo`, and a
`draft_noise` knob on the ceiling-eval runner. Eval harnesses (`draft-bench`,
`ceiling-eval`) gained `--workers` process-pool parallelism (0 = all CPUs
minus one; results byte-identical to serial, pinned by tests).

**A. Deck-quality cost of noise** (draft duel, ground pilot, 600 games/pair,
resolution +/-0.04): `balanced` 0.595 field-avg vs `+rnd2` 0.570, `+rnd4`
0.575, `+rnd6` 0.548, `+rnd8` 0.549. Head-to-head vs clean balanced: 0.48-0.49
(k<=4), 0.44-0.46 (k>=6). **k<=4 is ~free deck-wise**; even `+rnd8` still
crushes greedy/weighted (0.65-0.68).

**B. B0 eval-time robustness** (b0_s{0,1,2}, 40 paired seeds x 25 games,
noisy draft on the PPO side only):

| eval draft | avg-hard3 | delta vs clean | 95% CI |
|---|---:|---:|---|
| balanced (clean) | 0.6571 | -- | (reproduces the 0.657 of record) |
| +rnd2 | 0.6528 | -0.004 | [-0.011, +0.003] |
| +rnd4 | 0.6459 | -0.011 | [-0.019, -0.003] |
| +rnd8 | 0.6402 | -0.017 | [-0.028, -0.006] |

The reactive net is already fairly deck-robust: 8/30 random picks costs only
~1.7 points. (480-cell grid, 19 workers, 49 min wall.)

**C. Training with diversified decks -- VERDICT: null.** `rnd4_s{0,1,2}` =
B0 recipe + `--draft-noise 4` (the ~free-deck-cost point), 800k zoo
(~13.5 min/seed), paired ceiling-eval vs `b0_s{0,1,2}` (40 x 25, 19 workers,
~23 min wall vs a serial half-day):

```
cand=0.649  B0=0.657  delta=-0.008  95% CI [-0.016, +0.001]
VERDICT: ceiling-confirmed
```

Robustness cross-check (same noisy-eval grid as B, on the rnd4 nets):
clean 0.6491; +rnd4 -0.005 [-0.013, +0.003]; +rnd8 -0.013 [-0.024, -0.001].
The noise-trained nets do degrade a touch less under deck noise (about half
B0's slope at k=4), but from a lower clean baseline -- B0 ties or beats them
at EVERY eval-noise level (0.657/0.646/0.640 vs 0.649/0.644/0.637). Deck
diversity in training buys nothing, not even on the noisy-deck axis it was
supposed to help.

**Study bottom line:** the draft-diversity lever is null, consistent with the
ceiling study's read that the reactive net's limit is planning-side (H1/H2),
not data-side. B0 remains the recipe of record; `--draft-noise`, `+rndK`
bench specs, and `--workers` parallel eval stay as tested, documented tooling.
`rnd4_s{0,1,2}.zip` kept in runs/ for reference.

## E5 "planning-lite": vbeam own-turn beam planner -- H2 CONFIRMED (2026-07-02)

`locma/policies/vbeam.py` (branch feat/vbeam-turn-planner): at each decision
point, beam-search own-turn action *sequences* on a `_clone_battle` forward
model and score stopping points with B0's token critic, batched (one trunk
forward per beam depth). Fair by construction: the search never simulates
`Pass` (draws happen only at `start_turn`), so no hidden information is
touched; the evaluator reads only the public `BattleView`. Terminal win/loss
score +/-2 (outside the critic's [-1,1] clip), so a found lethal outranks any
estimate. Spec `vbeam:model_path,width,max_actions` (defaults 8/20, balanced
draft); `ceiling-eval` now accepts registry specs alongside .zip paths.
~0.6 s/game -- roughly 100x cheaper than netdmcts's K*I=1200 net calls.

**The bug that mattered (stop-scoring bias):** the naive planner scored
"stop here" with V(s). But V is a *state* value -- it already credits the
actions the net expects to still take this turn -- so "stop now" free-rode
on phantom actions. Probe: 13/38 turns planned as bare [Pass] (9 where the
reactive net itself would act); paired pilot 0.308 vs B0 0.652 (-0.343).
Fix: a stop is scoreable with V(s) only where the policy's masked argmax IS
Pass (continuation == passing, bias vanishes) or Pass is forced (line
exhausted); elsewhere the root stop survives only as a -1.5 fallback that
outranks self-inflicted losses (-2) and nothing else. Post-fix probe: 4/46
bare-Pass turns, healthy 1-7 action plans.

**Verdicts (paired vs b0_s{0,1,2}, standard ruler):**

```
pilot 10x10:  cand=0.854  B0=0.652  delta=+0.202  95% CI [+0.168, +0.241]
full  40x25:  cand=0.863  B0=0.657  delta=+0.206  95% CI [+0.196, +0.216]
VERDICT: headroom
```

**Reading:** H2 confirmed on the design doc's own terms -- the review's 0.70
bar is cleared by 0.16, and vbeam even beats the netdmcts teacher (0.817)
at ~100x less play-time compute. The ~0.66 plateau was never representation,
data, or optimization: it was within-turn plan composition, recoverable at
play time from the EXISTING B0 value head with zero retraining. Per
PPO-REVIEW-FB E5, "the reactive deployment constraint should be renegotiated"
-- vbeam costs well under a second per turn.

**Next levers (in order):** E5 variant 2 (fitted-value iteration: retrain the
critic against the planner's own play -- the current critic is evaluated
off-policy under vbeam); E5a (learned-potential PBS shaping with the frozen
critic); netdmcts with the vbeam-improved net as a stronger teacher for E4.

## E5 variant 2: fitted-value iteration on the vbeam critic -- VERDICT: null-to-negative (2026-07-02)

`locma/envs/vbeam_fvi.py` (branch feat/vbeam-fvi): `collect_value_data` shards
`record_practicum` over a process pool (the practicum's winner/seat columns ARE
the Monte-Carlo value labels, +1 if winner == seat else -1); `train_value_head`
fine-tunes ONLY the critic branch (`mlp_extractor.value_net` + `value_net`) on
frozen precomputed features. The policy path stays byte-identical (pinned by
test) -- vbeam's stop rule reads the policy head's masked argmax, so the output
is a drop-in `vbeam:out.zip` with identical stopping behavior.

**Data:** ~79k own-turn decision states per seed from 3,200 vbeam(b0_sX) games
each (4 zoo opponents x 400 seeds x 2 seats, 19 workers, ~3.5 min/seed, 0
failed games; training seeds 10k+, disjoint from the 1M+ eval range).

**Calibration trains beautifully; play does not** (paired pilots, 10x10,
candidates vbeam:fvi vs baselines vbeam:b0):

```
epochs=10: val MSE 0.53->0.29, sign acc 79%->91%
           cand=0.768  B0=0.854  delta=-0.086  95% CI [-0.103, -0.068]
epochs=2:  val MSE 0.53->0.33, sign acc 79%->89%
           cand=0.845  B0=0.854  delta=-0.009  95% CI [-0.024, +0.007]
```

**Mechanism (dose-response confirmed):** MC labels are constant within a game
-- every recorded state of a won game regresses toward +1. The beam does not
need "who wins this game" (sign accuracy); it needs ORDERING between sibling
states one action apart, and pushing all siblings toward the same target
actively erases that distinction. Calibration saturates by epoch 2; continued
optimization of the flattening objective damages ranking monotonically
(-0.009 -> -0.086). Even the light dose never gains: the MC objective is
wrong for a ranking evaluator, not under-optimized.

**Read:** E5 variant 1's +0.206 stands as the recipe of record
(vbeam:runs/b0_sX.zip). The PPO critic -- trained on diverse exploratory
rollouts with per-step GAE targets -- is a better *ranker* than a
better-calibrated outcome regressor. If a variant-2 retry is ever wanted, the
targets must differ between siblings: AZ-style backed-up plan scores from
`plan_turn` itself (grounded by searched terminals), TD/GAE targets, or an
explicit ranking loss on beam candidates. Tooling + verdict kept; models
`runs/fvi{,2}_s{0,1,2}.zip` and `runs/fvi-data-s*.npz` kept for reference.

## E5 variant 2b: AZ-style backed-up targets -- VERDICT: null (critic is already a fixed point) (2026-07-02)

The retry the 2a verdict asked for: targets that DIFFER between siblings.
`plan_turn` gained an optional `collect` sink -- each explored state gets the
best completed-plan score reachable through its action prefix, clipped to
[-1,1] (searched wins/losses ground it; the -1.5 root-fallback sentinel is
excluded). `collect_backup_data` harvests the states whose ranking decides
play (root + depth-1 siblings + stop-eligible states) from vbeam's own games;
`train_value_head` picks up the explicit `target` column automatically.

**The dataset itself delivered the verdict before the eval did:** ~78k
targets/seed from 800 vbeam(b0_sX) games each, and the pre-training val MSE
against them is 0.019 -- the B0 critic is ALREADY within ~0.14 RMS of its own
one-step beam backup on the planner's play distribution. Only ~5% of targets
are terminal-grounded (the "sign acc" column is that fraction, a metric
artifact for continuous targets). Fine-tuning has nothing to learn
(0.019 -> 0.016), and play confirms:

```
az10 pilot 10x10:  cand=0.847  B0=0.854  delta=-0.007  95% CI [-0.018, +0.004]
VERDICT: ceiling-confirmed
```

**The two flavors bracket the estimator, closing E5 variant 2:**

| targets | sibling-differing? | new information? | play delta |
|---|---|---|---:|
| 2a Monte-Carlo (10 ep) | no (constant per game) | yes (real outcomes) | -0.086 |
| 2a Monte-Carlo (2 ep) | no | yes | -0.009 |
| 2b one-step backup (10 ep) | yes | ~none (residual 0.019) | -0.007 |

MC targets carry real information but erase the sibling ordering the beam
ranks with; one-step backups preserve ordering but the critic already
satisfies them. Cheap critic retraining cannot improve the E5v1 planner:
**vbeam:runs/b0_sX.zip stays the recipe of record at 0.863.** A future
attempt needs multi-turn grounding without flattening (TD-lambda over planner
trajectories, or full AZ self-play where search depth spans the opponent's
reply). The nearer open levers are E4 (distill the 0.863 vbeam teacher --
cheaper AND stronger than netdmcts was) and E6/H3 (belief features).
Artifacts kept: `runs/fviaz-data-s*.npz`, `runs/fviaz{2,10}_s*.zip`.

## Artifact depot: checkpoints of record move to `depot:` refs (2026-07-02)

`runs/` had grown to 181 files / 1.2 GB of recipe-of-record checkpoints mixed
with dead experiments -- no provenance on model zips, and refs like
`vbeam:runs/b0_s0.zip` in this log had no guarantee the file still matched
what was measured. PR #58 added the versioned artifact depot (`docs/depot.md`):
git-committed index (provenance + sha256 + pin) over a gitignored
content-addressed store, `depot:` refs resolved at the registry/ceiling-eval
choke points, GitHub Releases as the (swappable) remote, `locma depot` CLI.

Everything this log cites as load-bearing is now published and pushed:

| depot artifact | was (runs/) | provenance highlights |
|---|---|---|
| `depot:b0` v1 | `b0_s{0,1,2}.zip` | reactive 0.657 / vbeam 0.863, recipe of record |
| `depot:cand1` v1 | `cand1_s{0,1,2}.zip` | ceiling study C1, -0.040 ceiling-confirmed |
| `depot:rnd4` v1 | `rnd4_s{0,1,2}.zip` | draft-noise arm C, -0.008 null |
| `depot:fvi-study` v1 | `fvi{,2}_s*.zip`, `fviaz{2,10}_s*.zip` | E5v2 bundle, parent b0@1, null/negative |

Canonical refs from here on: reactive `depot:b0/b0_sX.zip`, planner
**`vbeam:depot:b0/b0_sX.zip`** (0.863). Earlier entries' `runs/...` paths map
via the table above. The FVI npz datasets were NOT promoted (regenerable in
minutes from `depot:b0` + `locma/envs/vbeam_fvi.py` collectors); `runs/` is
now officially disposable. Second machine: `git pull && locma depot pull b0`.

## E4v2: distill the vbeam teacher + EXIT round -- VERDICT: null (plans do not compress into a reactive policy) (2026-07-03)

`locma/envs/vbeam_distill.py` (branch feat/vbeam-distill): `train_policy_head`
is `train_value_head`'s mirror image -- masked CE toward the planner's
recorded actions on frozen precomputed features, training ONLY
`mlp_extractor.policy_net` + `action_net` so the critic stays byte-identical
(pinned by test) and the output drops into `vbeam:` as the expert-iteration
arm. `behavior_clone` gained `init_model` warm-starts. vbeam is the first
FAIR teacher here (mcts/dmcts cheat; every vbeam decision is a function of
the public BattleView), so the PR #18 "info ceiling" excuse was gone going in.

**Data:** ~99k decision states per seed from 4,000 vbeam(b0_sX) game-sides
each (4 zoo opponents + vbeam self-play, 400 games/opp x 2 seats, seeds
20000+, ~5.4 min/seed at 19 workers, 0 failed games).

**Verdicts (full 40x25 ruler, paired vs b0 / vbeam:b0):**

| arm | val agreement | avg-hard3 | delta | 95% CI |
|---|---|---|---|---|
| B0 argmax vs teacher | 0.444 | 0.660 | -- | reference |
| PH (policy-head FT of B0) | 0.492 | 0.645 | -0.015 | [-0.024, -0.007] |
| BC (scratch) | 0.502 | 0.562 | -0.098 | [-0.106, -0.091] |
| FF (warm full FT, lr 1e-4) | 0.529 | 0.662 | +0.002 | [-0.006, +0.010] |
| **vbeam:PH (EXIT)** | -- | 0.8641 | **-0.0001** | [-0.0008, +0.0006] |

Round 2 (compounding) correctly skipped by the pre-registered trigger.

**Mechanism (three findings):**

1. **Agreement was never the binding metric.** The fair teacher lifted top-1
   agreement 0.37 -> 0.50 exactly as predicted, and play did not follow. Two
   structural reasons: (a) *label ambiguity* -- commuting action orders score
   identically in the beam and tie-break by insertion order, so within an
   equivalence class the recorded label is arbitrary and CE signal dilutes;
   (b) *fragile lines* -- the planner's +0.206 concentrates in rare precisely
   ordered sequences (found lethals, buff-then-attack chains). Executing
   those reactively needs near-perfect step fidelity; at 0.5 top-1 the
   student falls off the line mid-turn and reverts to B0-or-worse play.
   Imitation transfers the *typical* decision; the planner's value lives in
   the *exceptional* ones.
2. **The EXIT channel is empty by construction.** `plan_turn` expands all
   legal actions and ranks by critic alone; the policy head enters only via
   the `would_pass` stop rule. Pass-with-alternatives examples are ~0.3% of
   the data AND occur precisely where B0's argmax already IS Pass (that is
   what makes a stop scoreable), so distillation had zero signal to move
   stopping behavior. The full-eval CI [-0.0008, +0.0006] says the planner is
   empirically invariant to a policy-head shift -- tightest null this project
   has produced.
3. **B0's plateau is stable under imitation pressure.** FF (warm start) lands
   at +0.002 -- the CE objective had nothing to add to B0 at a lr that does
   not damage it; PH's -0.015 shows head-only optimization toward ambiguous
   labels slightly hurts; BC-from-scratch (0.562) beats the old cheater
   distills (~0.54) a bit, in line with the cleaner signal.

**Read (E4 closed):** the beam search is not compressible into this reactive
net by behavioral cloning at practical scale -- with FVI (E5v2) this closes
BOTH cheap distillation directions: the critic is already the fixed point of
its own backup, and the policy cannot absorb the plan distribution from
labels alone. `vbeam:depot:b0/b0_sX.zip` (0.863) stays the recipe of record;
the +0.206 is play-time compute, full stop. A future transfer attempt needs
objectives that carry *why* a line is better, not just *what* was played:
margin/ranking losses over beam siblings, Q-filtered imitation, or RL
fine-tuning against the planner. (Both baselines reproduced on the ruler this
run: reactive 0.660, vbeam 0.8642.) Artifacts: `runs/vdst-*` (data, models,
summary.json, overnight.log); tooling + tests merged with this branch.

## E7: shared draft variant -- first positive training-side CI, via the critic under vbeam (2026-07-03)

New engine rule variant (branch feat/shared-draft): **shared draft** -- a pick
REMOVES the card from the other seat's offer (second picker chooses from the
remaining 2, third card burned), first picker alternates by round. Breaks the
default rule's mirror where two seats running the same deterministic draft
build identical decks -- the asymmetric-deck generator the draft-determinism
hypothesis called for, without +rndK's deck-quality cost. Opt-in everywhere:
`start_draft(shared=)`, `run_game(shared_draft=)`, draft-bench/`duel --shared`,
`tournament --shared-draft`, `train`/`train-zoo --shared-draft`,
`ceiling-eval --shared-draft`; default path byte-identical; self-duel 0.500
calibration still exact. Driver: `scripts/shared_driver.py`; results
`runs/shared-summary.json`.

**A. Draft-bench, default vs shared** (field-avg; ground 600/pair, b0 net
300/pair): contested offers COMPRESS draft skill and reward first-pick
stat/Guard grabs over curve planning. Ground pilot flips the top:
balanced 0.654 -> 0.604 vs max-guard 0.592 -> **0.631** (new #1). Under the
b0 deployment pilot balanced stays #1 but its edge halves: 0.609 -> 0.579
vs max-guard 0.563, max-attack 0.494 -> 0.536.

**B. Tournament matrix/Elo** (8-policy roster, 100/pair): the battle-policy
hierarchy is UNCHANGED -- vbeam:b0 1825 -> 1840, dmcts 1750 -> 1801,
ppo:b0 1666 -> 1669, heuristics flat, random 683 -> 573 (a random drafter
suffers most when a competent opponent strips its offers; search exploits
deck asymmetry slightly better). The variant changes deck-building
incentives, not the policy pecking order.

**C+D. Training arm** -- `shared_s{0,1,2}` = B0 recipe of record +
`--shared-draft` (token V0, lr 1e-4, target_kl 0.025, 800k zoo, 13.6
min/seed). Paired verdicts (40x25 ruler, 19 workers, both baselines
reproduced: reactive 0.660, vbeam 0.8642):

| arm | deploy | cand | b0 | delta | 95% CI |
|---|---|---|---|---|---|
| reactive | standard | 0.663 | 0.660 | +0.003 | [-0.005, +0.011] null |
| reactive | shared | 0.659 | 0.638 | +0.021 | [+0.014, +0.028] real, sub-thr |
| **vbeam** | standard | **0.890** | 0.864 | **+0.026** | [+0.021, +0.031] real, sub-thr |
| **vbeam** | shared | 0.892 | 0.868 | +0.025 | [+0.020, +0.029] real, sub-thr |

**Reading (three findings):**

1. **Reactive: same story as every data-side lever.** Standard-ruler delta
   +0.003 -- deck diversity in training still buys the reactive net nothing
   (consistent with the draft-noise null). The shared-deploy +0.021 is pure
   distribution matching: B0 loses 0.022 moving to shared deployment
   (0.660 -> 0.638), shared-trained nets lose 0.004 -- training in the
   deployment environment recovers the shift, no new capability.
2. **vbeam: the first training-side change in the project with a solidly
   positive paired CI.** +0.026 [+0.021, +0.031] on the standard ruler (and
   +0.025 under shared deploy) -- sub-threshold by the pre-registered +0.03
   rule, but every prior training-side lever was null-or-negative with CIs
   straddling or below zero. The gain is invisible reactively (+0.003) and
   appears only under the planner: the shared-trained CRITIC ranks states
   better for the beam. Mechanism fit: shared-draft training exposes the
   value function to asymmetric deck matchups (varied material imbalances)
   the mirror-deck default never generates, sharpening exactly the ordering
   signal E5v2 showed the beam depends on -- a channel FVI could not improve
   directly (the critic was already the fixed point of its own backup; this
   changes the DATA, not the objective).
3. **vbeam:runs/shared_sX.zip = 0.890 is the best planner number measured**
   (vs 0.863 of record). Promotion to recipe of record is a judgment call:
   the +0.026 CI excludes zero decisively but misses the +0.03 bar the
   study pre-registered. If promoted, publish `depot:shared` with parent
   b0 provenance; a confirm-run at fresh seeds would de-risk it.

**Hypothesis scorecard** (the "draft determinism creates a moat" idea): the
moat framing stays wrong for the reactive net (finding 1, third null in a
row), but its kernel -- mirror decks starve the VALUE function of
asymmetric-matchup data -- was right, and the payoff routes through
play-time search. Deck asymmetry is a critic-data lever, not a policy lever.

Artifacts: `runs/shared_s{0,1,2}.zip`, `runs/shared-summary.json`,
`runs/shared-overnight.log`; tooling + tests on branch feat/shared-draft.

## E7 addendum: fresh-seed confirm + PROMOTION (2026-07-03)

Fresh-seed confirm run (same 40x25 shape, eval anchors moved to 2_000_000+,
disjoint from every seed range previously played): vbeam:shared 0.879 vs
vbeam:b0 0.853, **delta +0.026, CI [+0.020, +0.032]** -- the paired delta
replicates exactly on never-before-seen games (both absolute rates read ~1
point lower on this seed range; the pairing absorbs it).

**Promoted:** `depot:shared` v1 published (parent b0@1, pushed to
gh:depot/shared-v1). **Planner recipe of record is now
`vbeam:depot:shared/shared_sX.zip` (0.890)**, superseding vbeam:depot:b0
(0.863). Reactive recipe of record unchanged (depot:b0). Promotion rationale:
+0.026 misses the pre-registered +0.03 bar but the CI excludes zero decisively
on BOTH the original and a fully disjoint eval range; every prior
training-side lever was null-or-negative. Residual caveat: 3 training seeds
per arm (training-run noise not bootstrap-covered); a new-training-seed
replication is the remaining de-risk if this direction gets leaned on.

Mechanism discriminator queued: vbeam:depot:rnd4 (uniform-noise decks, never
evaluated under the planner -- the nets predate vbeam) vs vbeam:b0 on the same
ruler. rnd4 lifting vbeam = generic deck diversity is the driver; rnd4 null =
the shared draft's structured asymmetry (complementary contested halves ->
sustained advantage states for the critic) is the driver.

## E7 addendum 2: mechanism discriminator -- mirror-breaking itself is most of the effect (2026-07-03)

vbeam:runs/rnd4_sX.zip vs vbeam:depot:b0 on the standard ruler (the rnd4 nets
predate vbeam and had only ever been evaluated reactively, where they were
null -0.008): **0.883 vs 0.864, delta +0.018, CI [+0.013, +0.024]**.

Reading: uniform-noise decks ALSO improve the critic under the planner, so
the driver is substantially **generic mirror-breaking deck diversity**, not
only the shared draft's structured asymmetry. Shared retains a nominal edge
(+0.026 [+0.021,+0.031] vs +0.018 [+0.013,+0.024] -- CIs nearly disjoint, so
"structure adds ~+0.008" is suggestive, not confirmed) AND is the better
lever anyway: rnd4 costs deck quality (draft duel 0.575 vs balanced 0.595)
while shared decks are competently drafted.

Two consequences:
1. **The draft-noise study's arm-C "null" gets a retroactive footnote:** the
   rnd4 nets were better critics all along -- the 2026-07-02 study evaluated
   the only consumer (reactive play) that cannot express the improvement.
   Training-side levers must be scored under the planner from now on.
2. **Dose-response is the obvious next question:** if mirror-breaking is the
   active ingredient, shared+rndK, one-sided rndK, or stronger asymmetry may
   stack -- cheap to test now that the driver + --shared-draft/--draft-noise
   plumbing compose. Results: runs/shared-summary.json
   ("discriminator_vbeam_rnd4_standard").

## E7b: rndK dose-response -- mirror-breaking saturates fast; shared stays the best point (2026-07-04)

Fills the draft-noise depth curve under the planner (the E7 lesson: score
training levers under vbeam, not reactively). New arms rnd{8,12}_s{0,1,2} =
B0 recipe + --draft-noise k, paired 40x25 verdicts vs depot:b0 on the
standard ruler (driver scripts/rndk_driver.py, results runs/rndk-summary.json;
k=3 (~10%) deliberately skipped -- statistically indistinguishable from the
existing k=4 point):

| arm | noise | vbeam delta | 95% CI | reactive delta | 95% CI |
|---|---|---|---|---|---|
| b0 | 0% | 0 | (definition) | 0 | (definition) |
| rnd4 | 13% | +0.018 | [+0.013, +0.024] | -0.008 | [-0.016, +0.001] |
| rnd8 | 27% | +0.019 | [+0.013, +0.025] | -0.000 | [-0.006, +0.006] |
| rnd12 | 40% | +0.023 | [+0.018, +0.029] | -0.012 | [-0.020, -0.004] |
| **shared** | contested | **+0.026** | [+0.021, +0.031] | +0.003 | [-0.005, +0.011] |

Findings:
1. **The critic gain saturates almost immediately**: k=4 already captures
   ~+0.018 and doubling to k=8 adds nothing. A small amount of deck
   asymmetry unlocks the effect; more diversity does not scale it.
2. **No collapse even at 40% noise** -- vbeam delta holds (nominally rises
   to +0.023; CIs overlap k4/k8, unconfirmed drift). The critic benefits
   from advantage-variance regardless of absolute deck quality.
3. **The reactive tax finally appears at k=12** (-0.012, CI excludes zero):
   training on heavily degraded decks costs the policy head while still
   helping the critic -- the two heads want different data.
4. **Promotion stands.** Shared remains the best measured point: nominally
   highest planner delta AND the only mirror-breaking arm with zero
   reactive cost (its decks are competently drafted). No rndK arm beats it.

Open next probes (descending value): shared+rndK stacking (does noise on
top of contested drafts push past +0.026?); new-TRAINING-seed replication
of the shared arm (the remaining de-risk); one-sided rndK (pure handicap
asymmetry). Models rnd{8,12}_s* kept in runs/ (reference, regenerable --
not depot-published; no promotion).

## E7c: shared+rnd4 stacking -- no additivity; mechanism fully pinned (2026-07-04)

The one configuration that could have beaten the promoted +0.026: contested
offers (structure) + rnd4 (variance) at once. shrnd4_s{0,1,2} = B0 recipe +
--shared-draft --draft-noise 4 (the wrappers compose with zero plumbing;
driver scripts/stack_driver.py, results runs/stack-summary.json):

| verdict (40x25, standard ruler) | delta | 95% CI |
|---|---|---|
| vbeam:stack vs vbeam:b0 | +0.022 | [+0.016, +0.028] |
| **vbeam:stack vs vbeam:shared (direct)** | **-0.004** | [-0.007, +0.001] null |
| reactive stack vs b0 | **-0.028** | [-0.035, -0.020] |

Findings:
1. **No additivity.** The stack lands inside the E7b plateau (+0.022) and
   the direct paired comparison against shared is null-to-slightly-negative.
   With E7b this fully pins the mechanism: the critic gain is
   MIRROR-BREAKING, it saturates at the first sufficient dose, and shared
   draft (which supplies it at zero deck-quality cost) was already the
   optimum. Stacking is closed.
2. **The reactive tax is super-additive.** shared alone +0.003, rnd4 alone
   -0.008, stacked -0.028: under contested offers a wasted random pick also
   hands the opponent a free contested card, so noise costs the POLICY far
   more than in the default rules -- while the critic gain survives intact.
   The sharpest evidence yet that the policy and value heads want different
   training data.

**vbeam:depot:shared/shared_sX.zip (0.890) stays the recipe of record.**
The remaining open item from E7 is the new-TRAINING-seed replication of the
shared arm (bootstrap covers eval noise only). A speculative future lever,
given finding 2: decouple the heads -- train the critic on mirror-broken
games and the policy on clean ones (two-phase or dual-env training).
Models shrnd4_s* kept in runs/ (reference, regenerable; no promotion).

## E8: zero-training trio -- critic ensemble clears the bar; width curve pins the bottleneck (2026-07-04)

Three studies squeezing the EXISTING nets, no training (branch
feat/vbeam-zero-training, driver scripts/zero_driver.py, results
runs/zero-summary.json). New code: EnsembleValueEvaluator (mean-of-critics
beam ranking, spec vbeam:p1|p2|p3,width -- clip-then-mean values, mean-probs
argmax for the stop gate).

**1. Width sweep** (paired vs vbeam:shared w=8 = 0.890, full 40x25 ruler):

| width | avg-hard3 | delta | 95% CI |
|---|---|---|---|
| 4 | 0.8821 | -0.0078 | [-0.0104, -0.0053] |
| 8 | 0.8899 | -- | reference |
| 16 | 0.8921 | +0.0022 | [+0.0004, +0.0040] |
| 32 | 0.8949 | +0.0050 | [+0.0029, +0.0070] |

Log-shaped and nearly exhausted: each width doubling buys ~+0.002, 4x compute
for +0.005. The beam already extracts nearly everything the critic's ordering
signal contains -- **the planner is evaluator-limited, not search-limited**.
Investing in search depth (2-ply, wider beams) is the wrong axis; critic
quality is the binding constraint.

**2. Critic ensemble** -- rank with the mean of the three shared critics
(vbeam:shared_s0|s1|s2), paired vs the single-critic recipe of record:

```
ens 40x25: cand=0.9263  shared=0.8899  delta=+0.0364  CI [+0.0311, +0.0417]
VERDICT: headroom
```

**First headroom verdict since E5 itself, and it costs zero training** --
pure variance reduction on exactly the signal the width sweep just identified
as the bottleneck. Larger than E7's entire retraining gain (+0.026). Cost:
3x evaluator compute (~3x game time, still well under 2s/turn). Note the
three seeds stop being replicates and become one deployment artifact; a
promotion should confirm on fresh eval anchors (2M+) like E7 did.

**3. Retro-scoring** shelved checkpoints under the planner (vs vbeam:b0 =
0.863; pre-registered gate: 10x10 pilot, promote to full ruler iff pilot
ci_hi > 0):

| arm | pilot delta | pilot CI | full 40x25 |
|---|---|---|---|
| selfplay-r2 | -0.024 | [-0.052, -0.001] | not promoted |
| az-net-0 | -0.031 | [-0.061, -0.002] | not promoted |
| sweep-C (big net) | -0.108 | [-0.132, -0.084] | not promoted |
| vdst-ff_s{0,1,2} | -0.026 | [-0.042, -0.009] | not promoted |
| cand1_s{0,1,2} | +0.002 | [-0.009, +0.014] | **+0.0149** [+0.0093, +0.0204] |

No hidden treasure: self-play, AZ training, the big net, and the distill
full-FT all made the critic WORSE (az-net-0 consistent with E5v2 -- MC
outcome targets flatten sibling ordering; sweep-C decisively so). The one
survivor is another policy/value dissociation: cand1 was -0.040 REACTIVELY
(shelved for it) yet its critic is +0.015 better than b0's under the planner
-- sub-threshold, but the third independent instance (after rnd4 and shared)
of "reactive verdicts do not measure critic quality".

**Read:** the zero-training axis converges with E7: everything now points at
the critic. Recommended next steps, in order: (a) fresh-anchor confirm +
promote the 3-critic ensemble (0.926); (b) ranking-loss critic fine-tune on
shared data (the untried E5v2 survivor) -- ensembling and a better single
critic likely compose; (c) drop 2-ply/width plans (wrong axis, settled).
Models: no new artifacts (the ensemble IS depot:shared v1's three members).

## E8 addendum: fresh-anchor confirm + PROMOTION (2026-07-05)

The E7 promotion protocol applied to the ensemble (driver stage
ensemble_confirm_2M -- same arms, disjoint 2M+ eval-seed anchors, full 40x25):

```
main   (1M+ anchors): cand=0.9263  shared=0.8899  delta=+0.0364  CI [+0.0311, +0.0417]
confirm (2M+ anchors): cand=0.9213  shared=0.8789  delta=+0.0424  CI [+0.0372, +0.0480]
VERDICT: headroom (both) -- replicated, if anything larger on fresh anchors
```

**PROMOTED.** The planner recipe of record is now the 3-critic ensemble

    vbeam:depot:shared/shared_s0.zip|depot:shared/shared_s1.zip|depot:shared/shared_s2.zip

(width 8, avg-hard3 0.926 / 0.921 across the two anchor sets), superseding
single-critic vbeam:depot:shared (0.890). No new artifacts: the ensemble IS
depot:shared v1's three members consumed jointly -- promotion is spec +
documentation (baseline.md "Recipes of record -- 2026-07-04"). Deployment
cost: 3x evaluator compute, still ~2s/game. Caveat recorded: the three
training seeds are now one deployment artifact, so the "3 seeds/arm"
replication convention no longer covers training-seed noise for the recipe
itself -- a future de-risk is retraining 3 fresh shared seeds and re-running
the ensemble verdict (subsumes E7's open new-TRAINING-seed item).

## E9: distill the critic ensemble into one checkpoint -- VERDICT: null (values transfer, orderings do not) (2026-07-05)

Can the promoted 3-critic ensemble (+0.036/+0.042) be compressed to a single
checkpoint (1x evaluator compute)? Best-conditioned distillation attempt the
project has made: targets = ensemble MEAN values (sibling-differing, unlike
E5v2a MC labels; teacher != student fixed point, unlike E5v2b; noise-free,
pure forward passes), sampled on exactly the beam query distribution
(root + depth-1 siblings + stop-eligible states from vbeam-on-the-ensemble
play; new `collect_ensemble_data` in vbeam_fvi.py). Critic-branch-only FT of
each shared_sX (policy path byte-identical). Branch feat/ensemble-distill,
driver scripts/ensdist_driver.py, results runs/ensdist-summary.json.

**Data + fidelity gate (pre-registered: pooled val RMSE < mean cross-critic
spread):** 34,138 states, mean spread 0.1105; val MSE 0.021->0.0069 (10 ep),
pooled RMSE 0.0818 -> gate PASSED (residual ~55% of removed variance).

**Verdicts (full 40x25 ruler):**

| pairing | cand | base | delta | 95% CI |
|---|---|---|---|---|
| vdens vs vbeam:ensemble | 0.8924 | 0.9263 | **-0.0339** | [-0.0395, -0.0281] |
| vdens vs vbeam:shared | 0.8924 | 0.8899 | +0.0026 | [-0.0008, +0.0062] null |

The distilled critics land exactly back on the single-critic baseline:
~7% of the ensemble gain retained, n.s.

**Mechanism (the sharp lesson):** the gate passed and play did not move --
matching the teacher's VALUES to 0.08 RMSE is not matching its ORDERINGS.
The beam consumes sibling margins far finer than 0.08; residual regression
error re-randomizes exactly the comparisons that decide plans. This
generalizes E5v2a beyond label quality: even noise-free, sibling-differing,
query-distribution targets fail under a value-MSE objective. The fidelity
gate was calibrated to label variance when the operative scale is the
sibling-ordering margin -- a correctly-designed gate would compare residual
error against sibling VALUE GAPS. (Alternative co-explanation, untested:
frozen-feature capacity -- one extractor may not span the 3-extractor mean.)

**Read:** the ensemble stays the recipe of record at 3x compute; its benefit
is not a smoother value function you can regress onto but a de-noised
ORDERING. Two escalations preserved for a retry: (a) pairwise ranking loss
on actual beam-sibling pairs toward the ensemble's preference (the E5v2
survivor, now with a teacher that demonstrably ranks better -- fixes the
objective); (b) unfreeze the extractor (fixes capacity, costs the
byte-identical policy path). Artifacts: runs/vdens_s{0,1,2}.zip +
runs/ensdist-data.npz kept for reference; no promotion.

## E10: adversarial exploit benchmark -- the reactive net IS exploitable; the planner is not (2026-07-05)

User hypothesis: even the best policy falls to simple hand-designed strats --
(a) random deck, (b) high-attack minions behind Guards, (c) green buffs then
face, (d) board preservation. Built ShellBattlePolicy (priority script:
lethal > green buff > guard clear preferring killing survivors > winning
trades only > Guards-first summons > face > removal) + ShellDraftPolicy
(tanky Guards, high-attack threats, curve-aware) in
locma/policies/exploits.py; registry archetypes rnddeck / guardwall /
bufface / boardkeep / shell. Branch feat/exploit-bench, driver
scripts/exploit_driver.py, results runs/exploit-summary.json.
2,000 mirrored games per pair (Wilson 95% CI half-width ~0.02).

**The matrix (exploit win rate):**

| exploit | greedy | max-guard | max-attack | ppo:b0 | vbeam:shared | vbeam:ens (RoR) |
|---|---|---|---|---|---|---|
| rnddeck | 0.396 | 0.334 | 0.475 | 0.243 | 0.066 | 0.049 |
| guardwall | 0.704 | 0.670 | 0.688 | 0.472 | 0.176 | 0.129 |
| bufface | 0.672 | 0.641 | 0.654 | 0.446 | 0.176 | 0.124 |
| **boardkeep** | 0.757 | 0.711 | 0.736 | **0.540** | 0.250 | 0.189 |
| shell | 0.678 | 0.678 | 0.694 | 0.477 | 0.222 | 0.159 |

**Findings:**

1. **Hypothesis CONFIRMED at the reactive rung.** boardkeep (balanced deck +
   winning-trades-only) beats the reactive recipe of record: 0.540
   [0.519, 0.562]; guardwall and shell sit at near-parity (0.47). A
   hand-written script defeats B0 -- and boardkeep's implied avg-hard3
   (~0.735) is ABOVE B0's 0.657 on the same three baselines.
2. **Hypothesis REFUTED at the planner rungs.** vbeam:shared holds every
   archetype to 0.07-0.25; the ensemble to 0.05-0.19. The wall is
   instructive: guardwall 0.47 vs reactive -> 0.18 vs planner -- the beam
   composes exactly the multi-step wall-dismantling lines the reactive net
   fumbles. Exploitability was a PLANNING deficit, not a knowledge deficit.
3. **The ensemble strictly tightens every cell** vs the single critic
   (0.066->0.049, 0.176->0.129, 0.176->0.124, 0.250->0.189, 0.222->0.159):
   the +0.036 is robustness against out-of-distribution strategies too, not
   just avg-hard3.
4. **Discipline beats gimmicks.** boardkeep (no wall, no buffs, just value
   trades on a balanced deck) dominates the full shell everywhere -- the
   guard/green-heavy draft costs more deck quality than the wall returns.
   The exploit power against reactive nets is trade discipline.
5. rnddeck never threatens anyone (0.049-0.475): a random deck is a bigger
   handicap than any distribution surprise it creates.

**Read:** the deployed recipe (vbeam ensemble) is robust to this exploit
family; the reactive net alone should not be shipped where adversaries
adapt. Cheap follow-up lever if reactive hardening ever matters: boardkeep
into the training zoo (it is exactly the opponent B0 never sees -- a
disciplined non-suiciding trader). Archetypes are registered policies, so
draft-bench / tournament / web-panel can use them directly.

## E11: boardkeep into the training zoo -- hardening is PARTIAL, but the critic likes the data (2026-07-05)

Branch `feat/zoo-boardkeep` (stacked on `feat/exploit-bench`), driver
`scripts/zoohard_driver.py`, results `runs/zoohard-summary.json`.

E10 recorded the lever: reactive B0 loses outright to the scripted
`boardkeep` archetype (0.540) it never trains against. E11 executes it:
`ZOO_OPPONENTS` gains `boardkeep` as the fifth (final) phase and the B0
recipe of record is retrained from scratch, 3 seeds x 1M steps (~33 min
each on the 4080, cuda + 16 envs). The +200k budget is part of the
treatment. Pre-registered gates: hardening = boardkeep ci_hi < 0.5 vs
b0k_s0; strength = ruler ci_lo > -0.03; promote iff both.

**Exploit re-bench (E10 protocol, same 5M+ seed range = common random
numbers; E10-vs-B0 row in parens):**

| exploit | vs b0k_s0 | (vs b0_s0, E10) |
|---|---|---|
| rnddeck | 0.262 | (0.243) |
| guardwall | 0.469 | (0.472) |
| bufface | 0.437 | (0.446) |
| **boardkeep** | **0.514 [0.492, 0.535]** | (0.540 [0.519, 0.562]) |
| shell | 0.470 | (0.477) |

boardkeep vs the other seeds: s1 0.520 [0.498, 0.542], s2 0.503
[0.481, 0.524]. Pooled ~0.512 over 6,000 games.

**Ruler and planner arms (fresh 3M+ eval seeds):**

- reactive [b0k_s0..2] vs [depot:b0 s0..2]: **+0.0084 [+0.0013, +0.0157]**
  (0.683 vs 0.675) -- a small but significant avg-hard3 GAIN, not just
  non-regression.
- vbeam:b0k vs vbeam:depot:b0 (pilot +0.0167 promoted): full
  **+0.0186 [+0.0132, +0.0238]** (0.886 vs 0.868).

**Gates: hardening FAIL, strength PASS -> no promotion.**

Findings:

1. **Hardening is partial, not complete.** 200k steps against boardkeep
   pulls the exploit from a significant win (0.540) back to statistical
   parity (0.503-0.520 across all three seeds; every CI straddles 0.5),
   but no seed clears the pre-registered ci_hi < 0.5 neutralization bar.
   The reactive net learns to not LOSE to the disciplined trader; it does
   not learn to beat it. Consistent with the E10 mechanism: refuting
   boardkeep takes composed multi-step punishes, which a reactive policy
   cannot represent -- more gradient against the same opponent buys
   parity, and the rest is a planning deficit.
2. **No spillover within the family.** guardwall/bufface/shell are flat
   (0.472->0.469, 0.446->0.437, 0.477->0.470): the net hardened against
   the specific opponent in the zoo, not the strategy family.
3. **The strength ruler moved UP** (+0.0084 significant): the boardkeep
   phase is free avg-hard3, no robustness/strength trade-off at this dose.
4. **The critic is the real winner.** Under the planner, b0k beats b0 by
   +0.0186 -- vbeam:b0k lands at 0.886, statistically at the shared
   critic's 0.890, from DEFAULT-draft training. Second independent
   confirmation of the E7 lesson (value heads want harder/more diverse
   data), via a different diversity source (a disciplined opponent instead
   of asymmetric decks).

**Read:** boardkeep stays in the zoo (free strength + parity vs the
exploit + a better critic), but b0k does NOT replace depot:b0 as the
reactive recipe of record per the gates, and no depot publish happens.
Open lever recorded, not scheduled: b0k critics as ensemble members or as
a 4th member alongside shared_s0..2 (E8 showed ensembling de-noises
orderings; b0k's diversity axis is orthogonal to shared's).

## E12: b0k promoted to reactive recipe of record; mixed ensembles -- saturated at 3 critics (2026-07-05)

Branch `feat/zoo-boardkeep` (PR #71), driver `scripts/mixens_driver.py`,
results `runs/mixens-summary.json`. Two questions: (1) does b0k earn the
reactive recipe-of-record swap E11's neutralization gate withheld, and
(2) do the b0k critics -- whose diversity axis (disciplined opponent) is
orthogonal to shared's (asymmetric decks) -- improve the 0.926 planner
ensemble?

**(1) Reactive promotion: YES.** Fresh-anchor confirm [b0k_s0..2] vs
[depot:b0], 40x25 at 4M+ seeds: **+0.0227 [+0.0168, +0.0287]** (0.675 vs
0.652) -- second independent CI-positive range (E11's 3M+ gave +0.0084).
With the E11 exploit and critic dominance this is a clean dominance swap
(the +0.03 headroom bar is NOT claimed). Published `depot:b0k` v1
(gh:depot/b0k-v1, parent b0@1); baseline.md gains the 2026-07-05 section.
The E11 hypothesis gate (neutralize boardkeep) stays failed -- the swap is
"strictly better artifact," not "exploit closed."

**(2) Ensemble arms vs the RoR (`vbeam:shared_s0|s1|s2`, all paired
40x25 at 4M+, pilots 10x10 first):**

| arm | members | compute | full delta | 95% CI |
|---|---|---|---|---|
| ens_b0k3 | b0k x3 | 1x | -0.0053 | [-0.0107, -0.0000] |
| ens_mixed3 | shared_s0, shared_s1, b0k_s0 | 1x | -0.0010 | [-0.0065, +0.0043] |
| ens6 | shared x3 + b0k x3 | 2x | **+0.0085** | [+0.0040, +0.0132] |

ens6 fresh-anchor confirm (6M+): **+0.0177 [+0.0122, +0.0230]** (0.9375 vs
0.9198) -- replicated, larger point estimate, the highest arm mean measured
in the project so far.

**Gates: ens_b0k3 and ens_mixed3 not CI-positive; ens6 confirmed real but
below the pre-registered +0.03 bar for 2x evaluator compute -> the 3-critic
shared ensemble stays planner recipe of record.**

Findings:

1. **The ensemble benefit saturates at 3 critics.** Substitution is a wash
   (mixed3 exact null: a b0k critic is as good an ensemble member as a
   shared one) and the b0k-only trio ensembles fine (0.886 singles ->
   0.915) but from a lower base. The de-noising win is mostly spent by the
   third member -- same log shape as the E8 width curve, on the member axis.
2. **But it is not EXHAUSTED: ens6 is the first confirmed-on-both-ranges
   gain over 0.926** (+0.0085 / +0.0177, pooled ~+0.013). Doubling members
   from two orthogonal training-diversity families buys about a third of
   what the first ensemble bought. If deployment compute is free, ens6 at
   ~0.93-0.94 is the strongest known configuration; per the pre-registered
   economics it does not displace the RoR.
3. Cross-family diversity adds only marginally over within-family: the
   different data regimes (shared draft vs boardkeep zoo) produce critics
   whose ERRORS still mostly overlap -- consistent with E9 (the signal being
   averaged is fine sibling-margin noise, and most of it is common).

**Read:** reactive rung is now depot:b0k (0.683); planner rung unchanged.
Open levers, recorded not scheduled: ens6 as a "compute-rich" alternate
recipe if game cost ever stops mattering; fresh shared seeds retrain
(training-seed de-risk, E8 caveat) could piggyback new ensemble members.

## E13: boardkeep zoo + shared draft -- NO STACK; the diversity lever is one saturable resource (2026-07-06)

Branch `feat/bks-stacking` (stacked on feat/zoo-boardkeep), driver
`scripts/bks_driver.py`, results `runs/bks-summary.json`.

Question: the two training-side critic levers reach ~the same place from
different mechanisms -- shared draft (deck asymmetry, vbeam:shared 0.890)
and the boardkeep zoo (opponent discipline, vbeam:b0k 0.886). E7c ruled out
stacking two deck-asymmetry levers (shared+rnd4); cross-mechanism stacking
was still open. bks = B0 recipe + boardkeep-extended zoo + shared_draft=True,
3 seeds x 1M (~33 min each).

**Verdicts (paired 40x25, fresh 7M+ anchors):**

| arm | delta | 95% CI | means |
|---|---|---|---|
| vbeam:bks vs vbeam:depot:shared (PRIMARY) | +0.0023 | [-0.0024, +0.0070] | 0.882 vs 0.880 |
| reactive bks vs depot:b0k | -0.0024 | [-0.0099, +0.0048] | 0.672 vs 0.674 |

Pilot +0.0011 [-0.021, +0.020] agreed. **Gate: NO STACK** -- the conditional
ens(bks x3) arm never ran (stage B not CI-positive), no confirms needed.

Findings:

1. **The levers do not compose.** Combined training lands exactly where
   each lever lands alone (0.882 ~ 0.886 ~ 0.890, all within noise of each
   other, all ~+0.02 over vbeam:b0's 0.863-0.868). The reactive side is
   equally flat (-0.0024 n.s. vs b0k -- shared-draft training neither costs
   nor buys reactive strength, replicating E7's transfer null).
2. **Three independent measurements now triangulate one mechanism:**
   (a) each diversity lever alone gives ~+0.02 critic gain (E7, E11);
   (b) their critics substitute 1:1 in an ensemble with a null mixed arm
   (E12) -- the errors overlap; (c) combining the training data gives zero
   additional gain (E13). The "training-data diversity" critic lever is ONE
   saturable resource, not a family of composable tricks: the first dose of
   any diversity source de-biases the mirror-trained critic, and further
   doses -- even from a different mechanism -- hit the same ceiling at
   ~0.886-0.890 single-critic.
3. E7c's "mirror-breaking saturates" lesson therefore generalizes: it is
   not specific to deck asymmetry. Single-critic training-side improvements
   above ~0.890 will need a different KIND of signal (e.g. the E9-recorded
   ranking loss toward planner preferences), not more data variety.

**Read:** recipes of record unchanged everywhere (reactive depot:b0k 0.683,
planner shared-ensemble 0.926). bks artifacts stay in runs/ only. E13
closes the training-data-diversity direction; the surviving training-side
lever is ordering-aware critic training (ranking loss), and the surviving
zero-training lever is ens6 (the compute-rich alternate).

## E14a: within-turn failure diagnostic -- the obstacle is branching, not memory (2026-07-06, branch feat/e14-diagnostic)

User hypothesis behind the study: the reactive PPO net, invoked several
times per turn, lacks intra-turn context ("which minions already attacked")
and should degrade as the turn progresses -- if true, a token-v2 obs variant
with turn-event features is the lever. Instrument: `scripts/e14_diag.py`
shadow study -- depot:b0k s0 plays 2000 games (250 seeds x 2 seats x HARD3 +
boardkeep, seeds 9M+), and at every contested decision the vbeam planner
(width 8, SAME net) is queried from the identical state. 60,968 decisions,
18,184 turns; raw per-decision records in `runs/e14a-raw-*.jsonl.gz`,
aggregates + pre-registered H1-H5 verdicts in `runs/e14a-summary.json`.
Same-net design means every systematic disagreement is pure search benefit;
regret is scored by the net's own critic, independently anchored by the
known +0.20 ruler gap (vbeam:b0k 0.886 vs reactive 0.683).

Pre-registered verdicts:

| H | hypothesis | verdict | evidence |
|---|---|---|---|
| H1 | intra-turn context (-> token-v2 obs) | **NOT SUPPORTED** | disagreement FALLS with within-turn index (0.64 -> 0.58 -> 0.53 -> 0.50 -> 0.42); first-deviation hazard falls too (0.64 -> 0.42 -> 0.27); attacked-minions gradient 1.12x, below the 1.3x bar |
| H2 | premature pass | not supported | 0.09% of turns; overextension 0 -- pass timing is a solved sub-skill |
| H3 | missed lethal (-> search deficit) | **SUPPORTED** | planner finds a forced win in 1425 turns; the net fails to convert **18.3%** of them (engine-verified, no critic involved) |
| H4 | critic degrades mid-turn | not supported | (v-z)^2 flat-to-falling with index in early/mid buckets; only the tiny late bucket (n=136) worsens |
| H5 | choice complexity (-> capacity/search) | **SUPPORTED** | disagreement 0.32 at 2-4 legal actions -> 0.74 at 5-8 -> 0.85 at 9-14 (2.6x bottom-to-top) |

Findings:

1. **The user's premise inverts: the hardest decision is the FIRST action
   of the turn**, when mana is unspent and the option space is maximal
   (mana-spent=0 bucket has the highest disagreement, 0.68). As the turn
   progresses and options collapse, the net aligns with the planner. The
   obs already carries what matters (the ready bit encodes
   attacked-this-turn); nothing to fix observation-side. Stage B (token-v2)
   is **not justified** -- killed before spending training compute.
2. **The per-turn cost of reactivity is large and mid-game-concentrated:**
   mean regret (planner end-state value minus actual, same critic) = 0.113
   overall; 0.19 in mid-game turns vs 0.07 early. The planner genuinely
   restructures 48% of turns (end state differs); 17% of first-action
   disagreements are harmless order permutations (P10: their regret is
   exactly 0.000, validating the probe).
3. **Two specific pathologies:** (a) 18.3% of available forced wins are
   left on the table by the reactive net -- a cheap play-time lethal-solver
   guard would close these for reactive deployments; (b) the net UNDERUSES
   items ~3.3x: at contested decisions the planner wants Use 1161 times,
   the net plays it 356 times (confusion matrix: AU 363 + SU 610 dominate).
   Attack-vs-summon ordering is the bulk of remaining disagreement
   (AS 4322 / SA 4208).
4. Critic side-notes: reliability curve is monotone but compressed at the
   low end (v in 0.2-0.3 -> 45% actual win rate), ECE 0.131; and 29-42% of
   the net's own actions DROP its own critic's value (P5) -- head
   incoherence that search papers over.

**Read: E10's "exploitability is a planning deficit" is now measured at
turn granularity -- the reactive net fails at turn-level combinatorial
choice (branching), not at within-turn state tracking.** The observation
space is not the limiting factor; obs-space levers for the reactive net are
a dead direction on this evidence (consistent with R5's token-v1 null, now
with a mechanism). Levers this map points at: play-time search (the
existing RoR), a lethal-guard micro-wrapper for reactive deployments, and
policy-head training signals aimed at high-branching first actions (the
ranking-loss family, E9) rather than more input features.

## E15: ranking-loss critic + AZ-v2 policy round -- BOTH STAGES RESOLVE NEGATIVE, with mechanism (2026-07-06, branch feat/e15-ranking)

The "close-ish MuZero": MuZero's training-loop shape (search-produced policy
+ value targets at scale) on our perfect simulator, per
docs/e15-ranking-az2-design.md (pre-registered before any run). New library
code: plan_turn collect now carries the reaching action prefix (sibling
groups reconstructable); locma/envs/vbeam_rank.py (grouped harvest,
margin-weighted RankNet critic FT, listwise policy FT); driver
scripts/e15_driver.py. Data: 154,158 states / 39,242 sibling groups /
266,848 ranking pairs from vbeam-on-ensemble self-play (~3x E9, 5 min on 19
workers). All artifacts runs/vrank_s*, runs/vpi_s*, runs/e15-summary.json.

**Stage 1 (E9 escalation (a), the load-bearing question): G1 FIDELITY GATE
FAIL -- the capacity explanation is confirmed.**

| seed | pair acc before | after | fine (<0.08) before -> after | bar |
|---|---|---|---|---|
| s0 | 0.8044 | 0.8324 | 0.7206 -> 0.7462 | 0.9022 |
| s1 | 0.8013 | 0.8203 | 0.7229 -> 0.7358 | 0.9007 |
| s2 | 0.8043 | 0.8123 | 0.7311 -> 0.7358 | 0.9022 |
| pooled | 0.8033 | 0.8217 | | **0.9017** |

Ten epochs of margin-weighted ranking loss on 267k beam-sibling pairs close
only 19% of the ordering gap (and pay for it: val MSE 0.017 -> 0.030 --
calibration traded for ordering, still not enough). With the OBJECTIVE now
ordering-aware and the data at the exact beam query distribution, the
residual explanation from E9 stands alone: **one frozen shared extractor
cannot represent the 3-extractor ensemble's sibling ordering.** The
single-critic fine-margin number is the sharpest artifact of the study: the
base critic orders sibling pairs with ensemble-margin < 0.08 at only
0.72-0.73 (coarse pairs: 0.91) -- that ~0.28 fine-margin error rate IS the
ensemble's edge, localized. G2/G3 killed per the pre-registered criterion
(never run). Stage 3 not triggered.

**Stage 2 (listwise policy round): worse than null -- imitation actively
hurts.**

| gate | pairing | delta | 95% CI | read |
|---|---|---|---|---|
| G4 | vpi_sX vs depot:b0k | -0.0311 | [-0.0389, -0.0233] | fails the absorption bar |
| G4-base | vpi_sX vs shared_sX (own base) | **-0.0167** | [-0.0238, -0.0095] | the FT itself is CI-NEGATIVE |
| G6 | vbeam:vpi vs vbeam:shared | -0.0004 | [-0.0011, +0.0004] | planner untouched (critic frozen) |

In-distribution the signal landed (held-out top-1 vs teacher 0.45 -> 0.53;
CE down ~0.13 on all seeds). Mechanism probes (400 games each, E14a
headline metrics): item underuse 1.96x -> 1.65x -- the targeted pathology
MOVED -- but missed-lethal rate worsened 12.5% -> 15.7% and on-policy root
disagreement did not move (0.635 -> 0.641). **The net learned to START the
planner's lines (more item plays) without being able to FINISH them (worse
lethal conversion), and the net effect on play is negative.** Plan prefixes
without plans are worse than consistent myopia -- this generalizes EXIT's
null (#62) into a signed result.

**Read: the training-side program is now closed with mechanisms, not just
nulls.** Six attempts to move the reactive/single-critic artifacts toward
the planner (self-play, AZ loop, EXIT, E9 value-MSE, E15 ranking loss, E15
listwise imitation) end in: (a) the critic side is REPRESENTATION-limited --
fixing it means unfreezing the extractor (recorded escalation; costs the
byte-identical policy path, risks forgetting, low expected value); (b) the
policy side is structurally unable to use search preferences without the
search. Recipes of record unchanged everywhere (reactive depot:b0k 0.683,
planner shared-ensemble 0.926). Play-time search remains the only
planning-strength lever this kit has ever confirmed; its cheap upgrades
(lethal-guard wrapper for reactive deployments, E14a; ens6 at 2x compute,
E12) are the surviving practical items.

## 2026-07-06 — E16a: spell-representation diagnostic (observability hypothesis)

**Question.** Is the reactive net's item underuse caused by the flat obs
under-specifying spells? Static census says the gap is real: `_card_block`
drops `player_hp`/`enemy_hp`/`card_draw` entirely (15/44 items and 29/121
creatures have such hidden effects); four blue items (153/154/156/160) look
like 0/0 no-ability BLANKS, and **153 Healing Potion / 154 Poison / 160 Minor
Life Steal are an exact 3-way observation alias with near-opposite effects**
— the net provably cannot play them differently.

**Harness finding first (changes the baseline number).** `ppo:` pairs the net
with `balanced`, whose `_ITEM_DISCOUNT=12` was tuned (2026-06-25 entry) to
*hide* the net's spell weakness — RoR decks carry ~0–2 items/30, and a smoke
run produced literally ZERO item candidates in 16 games. E14a's 3.3x was a
decision-count ratio on those item-starved decks. Measured per opportunity
(card affordable in hand at turn start), the real gap is **1.69x**
(net 0.226 vs plan 0.381), matching E15's mechanism probe (1.65–1.96x).

**Method.** `scripts/e16_spellrep.py` (E14a shadow pattern): depot:b0k s0 vs
HARD3+boardkeep, net seat drafts RANDOM (forces item exposure; planner oracle
= vbeam w8 on the same net, computed at each own-turn start from the identical
state, so net-vs-plan stays matched). 2000 games, seeds 13M+, 71,586
candidate observations. Raw: `runs/e16a-raw-*.jsonl.gz`; per-card:
`runs/e16a-percard.json`.

**Result: all four pre-registered reads NULL — the observability hypothesis
is dead.** Underuse ratio U = plan_rate/net_rate per visibility class:

| class | n | net | plan | U |
|---|---|---|---|---|
| creature_plain | 34,169 | 0.384 | 0.325 | 0.85 |
| creature_rider | 10,895 | 0.387 | 0.328 | 0.85 |
| item_visible | 16,528 | 0.239 | 0.412 | 1.73 |
| item_mixed | 7,405 | 0.203 | 0.312 | 1.54 |
| item_hidden_only | 2,589 | 0.205 | 0.383 | 1.87 |

- **R1 alias trio:** planner spread 0.027 ≈ net spread 0.036 (plan rates
  0.437/0.462/0.436) — even the planner, which SEES the effects via
  simulation, plays the three aliased blues near-identically. The aliasing is
  real but worthless: their values are close in practice.
- **R2 hidden concentration:** U 1.87 vs 1.73 (ratio 1.08, bar 1.5) —
  underuse is essentially UNIFORM across visibility classes.
- **R3 rider items:** inverted (1.54 < 1.73). **R4 creature riders:** exactly
  equal (0.847 vs 0.846) — hidden summon effects cost nothing behaviorally.
- **Top per-card gaps are all VISIBLE premium items:** Decimate net 0.16 vs
  plan 0.58, Tome of Thunder 0.26/0.65, Scroll of Firebolt 0.27/0.64, Vial of
  Soul Drain 0.20/0.55 — cards fully specified by the obs the net already has.
- Creatures show NO underuse (U 0.85 — the net plays cards *more* eagerly
  than the plan); the deficit is item-specific and value-conditional.

**Read.** The net underuses items it can see perfectly, exactly as much as
items it cannot see at all, and the one provable information hole (the alias
trio) turns out not to matter even to the oracle. Item value is conditional
on target interaction — computed by search, not readable off card features.
Gate `v2a_retrain` = **false**: the flat-v2 obs retrain (append
php/ehp/draw, or the decomposed-channel variant) is NOT warranted as an
underuse fix. Consistent with and sharpens the E14a/E15 closure: play-time
search remains the lever; representation was the last cheap alternative and
it is now excluded with data.

## 2026-07-06 — E17: draft item-discount sweep under the planner

**Question.** E16a left one cheap lever standing: the balanced draft's
`_ITEM_DISCOUNT=12` was tuned to the *reactive* net's spell weakness, yet the
registry pairs the same item-starved draft with the vbeam planner, which
converts items ~1.7x better per opportunity. Is the pairing stale — does the
planner want spell-richer decks?

**Method.** `item_discount` is now a parameter (`BalancedDraftPolicy`,
`vbeam:model,w,ma,disc`, `ppo:model,disc` specs; `scripts/e17_draftdisc.py`).
Dose ladder d3/d0/d-2/d-4/d-8 (negative = item bonus) = 1.65/2.5/3.6/5.5/11.0
items per deck vs control 0.76. Standard verdict protocol vs the 0.926
ensemble RoR, common 14M anchors, pilots 10x10.

**Result: NULL-to-NEGATIVE with a monotone dose-response — more items in the
deck hurt even the pilot that plays items best.**

| arm | items/deck | delta vs RoR | 95% CI |
|---|---|---|---|
| d3 | 1.65 | -0.005 | [-0.020, +0.007] |
| d0 | 2.48 | -0.005 | [-0.028, +0.015] |
| d-2 | 3.60 | -0.028 | [-0.053, -0.005] |
| d-4 | 5.53 | -0.028 | [-0.065, +0.007] |
| d-8 | 10.96 | -0.077 | [-0.120, -0.042] |

No arm passed the pilot gate; full/confirm never ran. Reactive guard-rail
(ppo:b0k, d3) -0.014 [-0.030, +0.001] — the June discount-12 tuning stands
for the reactive net too.

**Read.** Per-opportunity conversion (E16a) measured play-vs-hold value of a
card already in hand; a draft slot is a different question — an item's
one-shot effect competes with a creature's recurring board presence, and
loses at essentially every dose even under a pilot that uses items ~optimally.
The discount-12 pairing is NOT stale; it is correct for both pilots. Draft-
side spell enrichment is closed as a lever. The `item_discount` plumbing
stays (useful for future probes). Combined E16a+E17 read: the reactive net's
spell underuse is real but fixing it has no deck-side or obs-side route —
item play value is search-computed (play-time), and item deck value is
simply low in this pool.

## 2026-07-06 — E18a: gamma=1.0 retrain (ByteRL ablation transfer test)

**Question.** ByteRL (arXiv 2303.04096, COG 2022 LOCM winner, no search)
reports +7% from gamma=1.0 — and our reward is purely terminal, so 0.99
scales the win signal by ~0.55–0.74 by the time it reaches early decisions.
Does the cheapest transferable ByteRL ingredient move the b0k recipe?

**Method.** `scripts/e18a_gamma.py`: the b0k recipe of record (token V0,
lr=1e-4, target_kl=0.025, 5-phase zoo, 1M steps, cuda) with the single-factor
change gamma 0.99 -> 1.0, seeds 0–2. Standard ruler vs depot:b0k, 40x25 @
fresh 16M anchors; planner/critic pilot; confirm @ 17M gated on CI-positive.

**Result: CI-NEGATIVE.** Reactive -0.0223 [-0.0312, -0.0139] (0.6585 vs
0.6808); planner pilot -0.0139 [-0.0289, +0.0006], full skipped. No
promotion; `runs/e18a_g1_s*.zip` kept.

**Read.** The gamma ablation does not transfer out of ByteRL's regime (24
V100 / ~5900-core off-policy V-trace actor-learner, weeks of training). In
on-policy PPO at 1M steps the undiscounted value target is noisier early
and the budget cannot amortize it — gamma=0.99 is doing variance-control
work here, not eating signal.

## 2026-07-07 — E18b: learned draft beats the balanced draft — BOTH arms confirmed with headroom

**Question.** ByteRL's one distinctive untried ingredient: learned deck-
building. Every deployed recipe uses the hand-written BalancedDraftPolicy;
E17 only established that the best member of that scripted-scorer FAMILY is
the incumbent. Can a draft net trained by RL against a frozen battle pilot
beat the scripted scorer?

**Method.** New plumbing (`locma/envs/draft_env.py`, `train_draft`,
`MaskablePPODraftPolicy`, registry draft-override param): DraftEnv = 30-pick
episode, frozen battle pilot plays BOTH seats (mirror isolates deck signal),
terminal reward = mean win over 3 reshuffled playouts, gamma=1.0, 300k picks
(~10k episodes, ~75 min/seed). draft_s{0,1,2} each trained vs its matching
pilot depot:b0k/b0k_s{s} against a balanced-drafting opponent — exactly the
G1 matchup. `scripts/e18b_learndraft.py`; anchors 18M primary / 19M confirm.

**Result: draft_seam_open = true. Both gates pass full AND fresh-anchor
confirm, both with the +0.03 headroom verdict.**

| arm | delta | 95% CI | confirm (19M) | avg-hard3 |
|---|---|---|---|---|
| G1 reactive: ppo:b0k_sX,draft_sX vs ppo:b0k_sX | **+0.1166** | [+0.104, +0.129] | +0.1028 [+0.093, +0.113] | 0.675 -> **0.791** |
| G2 planner: vbeam:shared,8,20,draft_sX vs 0.926 RoR | **+0.0541** | [+0.047, +0.061] | +0.0473 [+0.042, +0.053] | 0.924 -> **0.978** |

Census (200 seeded drafts): learned decks carry 3.9–5.2 items/30 at mean
cost ~3.3 (balanced: 0.76 items, cost 4.82), with a sharply cheaper curve —
2-cost bucket 7.4 vs 4.2, 7+-cost 2.2 vs 6.5 — and full card diversity
(158–160 unique cards; no degeneracy).

**Read.** The scripted-scorer axis (E17) and the deck-value question were
answered for the wrong family: E17's dose ladder forced items into an
otherwise-unchanged balanced deck and lost monotonically; the learned draft
takes ~5 items AND wins big — because it simultaneously rebuilds the curve
around cheap cards. "More items" was never the lever; *which cards at which
curve* is, and that point of draft space is not expressible inside
BalancedDraftPolicy's scorer. Two more notable facts: (1) the deck
generalizes — trained vs balanced-draft opponents, it beats the HARD3
baselines' own drafts; (2) it transfers across pilots — tuned for the
reactive b0k, it lifts the shared-critic planner ensemble to 0.978, the
largest planner move since E5. Training-side RL is NOT dead (E15 closed
absorb-the-planner distillation, not RL on new decision surfaces); the
ByteRL end-to-end lesson survives contact, its gamma lesson (E18a) does not.

Open follow-ups recorded, not scheduled: promotion protocol for both
recipes of record (depot publish + baseline.md); retrain battle nets ON
learned-draft decks (they currently train on balanced decks — E7's
policy/value data split suggests gains); exploit-bench re-read (cheap-curve
decks vs archetypes); joint draft+battle fine-tune (the full ByteRL E2E),
now justified by G1/G2.

## 2026-07-07 — E18b addendum: PROMOTION — depot:ldraft, both recipes of record swap their draft half

Published `depot:ldraft` v1 (gh:depot/ldraft-v1, parent b0k@1): the three
E18b draft nets as ldraft_s{0,1,2}.zip. Sanity: both new specs resolve and
play via depot refs.

| role | recipe of record since 2026-07-07 | avg-hard3 |
|---|---|---|
| planner | `vbeam:<shared ens x3>,8,20,depot:ldraft/ldraft_sX.zip` | **0.978** |
| reactive | `ppo:depot:b0k/b0k_sX.zip,depot:ldraft/ldraft_sX.zip` | **0.791** |

Battle halves unchanged — both promotions swap only the Composer's draft
half, so every historical number stays reproducible (a spec without the
draft param still means balanced). Full verdicts + mechanism in the E18b
entry above; recipe details in docs/baseline.md 2026-07-07 section.

## 2026-07-07 — E18c: exploit re-read of the ldraft pair — guard-rail PASS, and boardkeep is finally neutralized

**Question.** The promotion changed the deployed DECKS radically (cheap curve,
4-5 items); the E10 archetypes were tuned against balanced-deck reactive
play. Does the new reactive pair open adversarial holes? `scripts/
e18c_exploit.py`, E10/E11 protocol exactly (20x50 mirrored blocks, 5M common
random numbers, Wilson 95% CI), defender = `ppo:b0k_sX,ldraft_sX`.

**Result: guardrail_pass = true — every archetype got WORSE, by 0.11-0.15.**

| archetype | vs b0k+balanced (E11) | vs b0k+ldraft (E18c) | delta |
|---|---|---|---|
| rnddeck | 0.262 | 0.153 [0.137, 0.169] | -0.110 |
| guardwall | 0.469 | 0.320 [0.299, 0.340] | -0.149 |
| bufface | 0.437 | 0.290 [0.271, 0.310] | -0.147 |
| boardkeep | 0.514 | 0.408 [0.387, 0.430] | -0.106 |
| shell | 0.470 | 0.342 [0.321, 0.363] | -0.128 |
| boardkeep vs s1/s2 | 0.520 / 0.503 | 0.386 / 0.377 | -0.134 / -0.126 |

**Read.** The ldraft deck closed holes instead of opening them. The headline:
boardkeep — E11's unfinished business (hardening only reached parity 0.512,
the pre-registered neutralization bar ci_hi < 0.5 FAILED) — now loses
clearly on all three seed pairs (0.38-0.41) with the SAME battle nets. The
cheap creature curve gives the reactive net the board presence to out-trade
the disciplined trader; deck construction did what E11's opponent-curriculum
training could not. E10's "exploitability = planning deficit" needs an
amendment: part of that deficit was DECK deficit. All archetypes now lose to
the reactive recipe of record; the exploit bench no longer has a positive
row against any deployed recipe.

## 2026-07-07 — E19: deck-distribution retrain — reactive NULL (the pilot is deck-robust), critic arm CI-positive small

**Question.** E18b's promotion left a train/deploy gap: the b0k battle nets
trained on balanced decks but now pilot ldraft decks. Does retraining the
recipe on the decks it deploys with (draft_override plumbing,
BothSeatsDraftPolicy) recover anything? `scripts/e19_deckretrain.py`; both
sides of every verdict use the SAME ldraft draft halves, isolating the
battle net. Models runs/e19_b0kl_s{0,1,2}.zip (~66 min/seed — the ldraft
inference in 16 env workers doubles the b0k training cost).

**Result: reactive NULL, critic real-but-small, guard holds.**

| arm | delta | 95% CI | absolute |
|---|---|---|---|
| reactive: ppo:b0kl_sX,ldraft_sX vs ppo:b0k_sX,ldraft_sX (40x25 @ 20M) | +0.0008 | [-0.006, +0.008] | 0.797 vs 0.796 |
| critic: vbeam:b0kl_sX,8,20,ldraft_sX vs vbeam:b0k_sX,... (40x25) | **+0.0101** | [+0.007, +0.013] | 0.969 vs 0.958 |
| boardkeep guard (20x50 @ 5M) | — | [0.368, 0.411] | 0.389 (E18c pair: 0.408) |

No promotion (reactive gate fails at the first hurdle; confirm never ran).

**Read.** The reactive pilot is deck-robust at recipe-of-record level — the
+0.117 E18b gain needed no matching retrain, and giving it one buys nothing.
This is the E7 policy/value split appearing a third time: the POLICY head
does not care where the decks come from, the VALUE head does (single-critic
planner +0.0101 CI-positive from deck-matched training data — real, but
under the +0.03 bar and still below the deployed shared-ens 0.978). The
E19b "alternating loop" premise weakens accordingly: another draft round
would face an unchanged pilot. Surviving lever recorded, not scheduled:
retrain the SHARED recipe (the mirror-breaking critic data) with
draft_override=ldraft and test that 3-ensemble against the 0.978 RoR — the
critic signal is where deck-matched data pays. Also standing: the ruler
saturation problem (planner reads now compress against HARD3 at 0.97+).

## 2026-07-07 — E20: draft-priority distillation — per-card FIT is null, but CENSUS heuristic and NET-ELICITED priority both recover real headroom, zero inference

**Question.** E18b's learned draft (+0.117 reactive / +0.054 planner) is a
neural net. Can the same gain be reproduced by a cheap, deployable heuristic —
a static per-card priority table combined with `BalancedDraftPolicy`'s
existing curve-need / creature-deficit context — with no net at draft time?
`scripts/e20_draftdistill.py`, three methods tried in sequence.

**Stage B (per-card fit against REAL revealed picks): NULL.** Logged every
pick `draft_s{0,1,2}` made across 900 episodes (27,000 decisions) vs a
balanced opponent; fit a per-card value `v[card_id]` (161 params) plus
`w_need`/`w_creature` context weights via multinomial logistic regression
(3-way softmax, matching `BalancedDraftPolicy`'s own scoring shape). Held-out
choice accuracy 50.0% (vs 33% chance) — some signal, but `w_need` fit
**negative** (-0.15), the wrong sign vs curve-filling intuition — a tell that
joint fitting against noisy in-episode context confounded card identity with
curve state. Pilot verdict: -0.013 [-0.06, +0.037] vs `ppo:b0k_s0` — recovers
NONE of the net's win-rate gain despite the above-chance accuracy. Same
capacity-wall shape as E9/E15: the mechanism is real but doesn't collapse
into this fit family.

**Stage D (hand-specified census heuristic, no fitting at all): POSITIVE.**
Recalibrated `BalancedDraftPolicy`'s two tuned constants directly from the
E18b census, no ML: `curve_target` = the 3-seed average learned-deck curve
rounded to sum 30 (`[2,4,7,4,5,3,3,2]` vs balanced's `[1,3,5,5,5,4,3,4]`);
`item_discount` swept on a grid until item output matched the census rate
under that curve — landed at **-2.0**, i.e. items needed a small BONUS, not
just a smaller penalty (discount=0 alone still undershot the census item
rate). Pilot: **+0.067 [0.042, 0.09]** vs `ppo:b0k_s0` — 42% of the net's
gain, from two recalibrated numbers.

**Stage F (net-ELICITED per-card priority, neutral context): the strongest
heuristic.** Diagnosed Stage B's failure as a context confound, not a
capacity wall — queried `draft_s{0,1,2}` directly with synthetic 3-card
offers at a FIXED neutral context (round 0, empty deck; no real episode, no
curve variation to confound with card identity), 8000 random triples/net,
24000 pooled, fit via the same Plackett-Luce softmax but with ONLY per-card
strength (no context terms — none needed, context never varies). Held-out
accuracy **72.1%**, far above Stage B's 50%, train/held-out nearly identical
(no overfit). Spliced the resulting card values onto Stage D's census
`curve_target` and `BalancedDraftPolicy`'s ORIGINAL context weights
(`w_need=3.0`, `w_creature=2.0` — not refit). Pilot: **+0.107 [0.057, 0.158]**
reactive (67% of the net's +0.160) and **+0.060 [0.037, 0.085]** under the
3-critic planner ensemble vs `vbeam:<ens>` (0.978 vs 0.918 absolute) — lands
right on E18b's own confirmed planner number. Top-priority cards are all
cheap (1-3 cost) creatures, split between efficient vanilla stat-lines and
Lethal/Ward keyword carriers — notably NOT Guard, which `BalancedDraftPolicy`'s
hand-tuned keyword weights rate highest.

**Read.** The net's draft edge decomposes into three legible, additive
pieces: an aggregate deck-shape shift (curve young + item allowance, ~40% of
the gain, hand-specifiable from the census alone with zero fitting), a
per-card ranking (recovers another ~27%, but ONLY when elicited in a fixed
context — fitting it jointly against real, context-varying picks fails
outright, the opposite of what you'd expect from "more realistic data").
Final algorithm: `score = elicited_priority[card] + 3.0*curve_need +
2.0*creature_deficit_bonus`. The remaining ~33% (reactive) is presumably
offer-relative/synergy reasoning not expressible in this additive form; under
the planner the heuristic apparently closes the gap entirely at pilot scale
(planner's E16a item-conversion advantage means deck shape alone captures
most of the value there). **Published `depot:edraft` v1** (parent
`ldraft@1`) — added to the range of usable draft policies (docs/baseline.md),
not a recipe of record (pilot-scale only, not full/confirm gated). Next step
if pursued: full 40x25 + confirm on both arms before considering promotion.

## 2026-07-08 — E21: advanced-opponent zoo pilot — swap `boardkeep` for an old-net+`edraft` opponent; reactive null, boardkeep guard REGRESSES

**Question.** Every zoo opponent to date (`ZOO_OPPONENTS`:
greedy/scripted/max-guard/max-attack/boardkeep) is a scripted heuristic; E11
already showed one such addition (`boardkeep`) buys parity against its own
exploit, never refutation. Untried: replace the curriculum's last phase with
an opponent that is not a hand-written archetype but the strongest cheap
net-based play available — the old `depot:b0k` battle net wearing E20's
zero-inference `depot:edraft` draft heuristic (costs nothing extra over a
normal `b0k` opponent, since `edraft` needs no net at draft time). A light
generational-self-play design: train gen N+1 against gen N wearing a better
deck. `scripts/e21_advopponent.py`.

**Treatment.** `ZOO_OPPONENTS[:-1] + (ppo:depot:b0k/b0k_sX.zip,depot:edraft/
e20-elicit-fit.json,)` — same 5 phases, same 200k/phase, same 1M total budget
as `depot:b0k`'s own recipe, so only the 5th phase's opponent IDENTITY
differs (no budget confound, unlike E11's own boardkeep addition which
extended the budget). Models `runs/e21_adv_s{0,1,2}.zip`.

**Result (pilot 10x10 vs `depot:b0k`; boardkeep guard full 2000 games,
E10/E11 protocol).** Reactive **NULL**: -0.0056 [-0.0283, +0.0172] — same
pattern as E19, the reactive net gets nothing from this opponent-diversity
swap. Planner/critic small CI-positive: +0.0133 [+0.0028, +0.0244] — smaller
than E7's shared-draft (+0.026) or E19's ldraft-critic (+0.010), plausibly
already inside E13's ~0.890 single-critic saturation ceiling, not new
headroom. Boardkeep guard **REGRESSED**: 0.5325 [0.5106, 0.5543] vs E11's
0.512 (explicit boardkeep training) — back near E10's original unhardened
0.540. Losing the archetype-specific phase cost the archetype-specific
hardening; the "advanced" opponent didn't generalize to cover it.

**Read.** Confirms E13's mechanism a fourth way: opponent/deck diversity
from ANY source (scripted archetype, asymmetric draft, or a stronger net
opponent) saturates the critic at the same ceiling and doesn't stack; a
"stronger" opponent doesn't beat a scripted archetype's specific coverage,
it just trades WHICH exploit gets covered. Lever closed — no full/confirm
follow-up (reactive null + guard regression + likely-saturated critic gain).
If opponent/deck diversity is worth revisiting it needs a different signal
KIND (ranking loss toward planner preferences, per E13/E15), not another
diversity source.

**Ops note.** Mid-run, 16-worker `SubprocVecEnv` torch imports started
failing with `WinError 1455` ("paging file too small") / `1114`. Root cause:
4 **orphaned Telegram-plugin `bun` server.ts processes**, leaked across
previous sessions since 2026-06-30 and never terminated, together holding
~94GB of committed virtual memory and starving the box's commit limit (same
class of bug as E6's Windows commit-limit note, different trigger — leaked
unrelated processes, not concurrent trainers). Diagnosed via `Get-Process |
Sort PagedMemorySize64 -Descending` (commit, not working set — the leak
showed huge commit with small working set). Killed all 4 (user-confirmed),
memory recovered (1.8GB -> 110GB free virtual memory), relaunch succeeded
cleanly.

## 2026-07-09 — E22: MCTS-depth pilot — search beats the 0.978 planner at a SHALLOW budget; depth, not information, is the driver

**Question.** How does the planner recipe of record (`vbeam:<3-critic shared
ensemble>,8,20,ldraft`, 0.978 avg-hard3, confirmed) hold up against real tree
search at the SAME draft, and how much search depth would MCTS need to win?
Required plumbing first: `mcts:`/`dmcts:` registry specs hardcoded
`GreedyDraftPolicy` with no override (unlike `ppo:`/`vbeam:`) — added a 5th
positional draft param to both (`locma/policies/registry.py`,
`_greedy_draft_param`; backward-compatible, old bare `mcts:100` specs stay
byte-identical). `scripts/e22_mcts_depth.py`.

**Design.** Direct head-to-head (`run_match`, mirrored, Wilson 95% CI, 200
games/cell after mirroring) rather than the avg-hard3-vs-HARD3-pool ruler
every other experiment here uses — the question is who wins when they
actually play each other, not who beats a common reference pool better
(non-transitive matchups can mislead on ratings/pool deltas — see the
"read the matrix" note elsewhere in this doc). Both sides use the SAME
draft: `depot:ldraft/ldraft_s0.zip` (the confirmed RoR draft, not the
pilot-scale `edraft`), isolating search quality as the only variable.
Candidate: `vbeam:<3-critic shared ensemble>,8,20,ldraft_s0`. Cheating MCTS
swept at iterations = 100/1000/5000/20000. Fair `dmcts` swept at K=15 fixed,
I (per-world iterations) = 30/100/500/2000 (total sim budget 450 -> 30000,
bracketing the same order of magnitude as the MCTS sweep; I=30 reproduces
the codebase's old "dmcts ~ as strong as cheating mcts" reference point as
the low anchor).

**Result.** Both searchers cross over and decisively beat the planner at a
budget far shallower than "in theory MCTS needs deep search" would suggest:

| search | budget | planner win rate | 95% CI |
|---|---|---|---|
| cheating MCTS | 100 iters | 0.545 | [0.476, 0.613] (toss-up) |
| cheating MCTS | 1,000 iters | 0.260 | [0.204, 0.325] (MCTS ahead) |
| cheating MCTS | 5,000 iters | 0.220 | [0.168, 0.282] |
| cheating MCTS | 20,000 iters | 0.170 | [0.124, 0.228] |
| fair dMCTS | K=15,I=30 (450 sims) | 0.580 | [0.511, 0.646] (planner barely ahead) |
| fair dMCTS | K=15,I=100 (1,500 sims) | 0.385 | [0.320, 0.454] (dMCTS ahead) |
| fair dMCTS | K=15,I=500 (7,500 sims) | 0.315 | [0.255, 0.382] |
| fair dMCTS | K=15,I=2000 (30,000 sims) | 0.295 | [0.236, 0.362] |

Crossover for cheating MCTS sits between 100 and 1,000 iterations; for fair
dMCTS between 450 and 1,500 total simulations — almost the SAME budget as
the cheater. Both plateau (diminishing returns) past ~5,000-30,000 sims, at
a 17-30% residual win rate for the planner, not zero.

**Why.** Cheating MCTS and fair dMCTS crossing over at nearly the same
budget means the hidden-information cheat barely matters here — the real
driver is search DEPTH. `vbeam` only beam-searches action sequences WITHIN
the current turn and scores stopping points with the value net; it never
simulates the opponent's reply or later turns. MCTS/dMCTS do real multi-turn
lookahead. Any genuine multi-ply search beats a single-ply search+critic at
a surprisingly cheap budget. The planner isn't hopeless (it keeps 17-30% of
games, presumably on favorable draws), but it is far from the strength of
even modest real search depth.

**Read / next.** This reframes the planner's ceiling: its "planning" is
shallower than it looks (one-turn beam, not a tree). The registry already
has `netdmcts` (net-guided determinized PUCT — multi-turn tree search using
the TRAINED value net instead of random rollout at the leaves) built but
never benchmarked against the current recipes; that is the direct next
question — can the depth advantage be had WITHOUT giving up the trained
critic. Not yet scheduled (E23 candidate).

**Addendum — how many turns deep is "N iterations", really?** Instrumented
`MCTSBattlePolicy`'s own tree-building loop (throwaway probe, not a repo
script) and sampled real decision states via `run_game`'s `on_pre_step` hook
across greedy/scripted/max-guard/max-attack/boardkeep games, to measure
rather than assume the conversion. Measured: branching factor ~5.1 legal
actions/decision (an older doc note's "~21" was stale, from a different
action-space era) and ~2.95 actions/turn (turns are short). Tree nodes ≈
iterations (confirms vanilla UCT: one new node per simulation). The
principal-variation depth (the most-visited-child line — the move actually
taken, not just the deepest node anywhere in the tree) grows
**logarithmically** in N, not linearly:

| iterations | tree nodes | PV depth (actions) | PV depth (turns) | + fixed 3-turn rollout tail |
|---|---|---|---|---|
| 30 | 31 | 3.5 | 1.2 | 4.2 |
| 100 | 99 | 4.4 | 1.5 | 4.5 |
| 500 | 482 | 5.9 | 2.0 | 5.0 |
| 1,000 | 951 | 6.5 | 2.2 | 5.2 |
| 2,000 | 1,880 | 7.9 | 2.7 | 5.7 |
| 5,000 | 4,625 | 9.5 | 3.2 | 6.2 |
| 20,000 | 18,090 | 11.3 | 3.8 | 6.8 |

`rollout_turns=3` is a FIXED cap (random/heuristic play past the tree
frontier, then a static leaf evaluation) that does not grow with N — so at
low iteration counts most of the total lookahead horizon is that crude
rollout tail, not real search; as N grows the balance shifts toward genuine
simulated depth, which is presumably why win rate keeps improving well past
the head-to-head crossover even though turns-deep grows slowly. This lands
directly on the crossover itself: cheating MCTS's toss-up-to-decisive
transition (100→1,000 iters) and fair dMCTS's same transition (I=30→100)
both sit at only **~1.2-2.2 turns of real simulated depth**. The planner
isn't losing to deep search — it's losing to barely more than one real turn
of genuine forward simulation, because it does zero (single-turn beam only).
Confirms "depth, not information" a second, mechanistic way — not just
budget-matched outcome parity between the cheater and the fair searcher, but
the actual measured tree shape underneath it.

**Ops note.** Pre-run cost estimate (based on a stale doc note, "~0.2s/game"
for `mcts:100`) was wrong by a large factor — the real run took ~4 hours
total (`dmcts:15,2000` alone: 106 min; `mcts:20000`: 79 min), not the
"under half an hour" estimated going in. Completed cleanly (no crash, no
repeat of E21's memory issue). Future search-depth sweeps at 5-figure
iteration/sim counts should budget hours, not minutes.

## 2026-07-13 — E26: play-time micro-guards — first reactive-rung HEADROOM since E18b, from zero training

**Question.** With training-side absorption closed (E15) and deep search
excluded by the deployment constraint, two recorded-but-never-built FAIR
play-time levers remained on the reactive rung: (a) the lethal-guard
micro-wrapper E14a called for (the net misses 18.3% of engine-verified
forced wins), and (b) the policy-head analog of E8's mean-of-critics
ensemble. Plus one draft-side open item: the E20 `edraft` full confirm.
Design pre-registered in `docs/e26-microguards-design.md`; branch
`feat/e26-microguards`; driver `scripts/e26_guards.py`; results
`runs/e26-summary.json`. Implementation by a subagent, reviewed
line-by-line.

**New machinery.** `locma/policies/lguard.py`: `find_lethal` — exhaustive
cap-bounded DFS over own-turn action sequences (never simulates `Pass`;
same forward-model fairness class as `vbeam`; view-dedup makes the explored
set a true closure, so one exhausted empty search soundly suppresses
re-search for the rest of the turn) + `LethalGuardBattlePolicy` (plays a
found forced win move-by-move, else delegates). `MaskablePPOEnsembleBattle-
Policy`: mean of members' masked action distributions, argmax. Specs:
`ppo:` now takes `|`-separated models; new `lppo:model[,draft[,node_cap]]`.

**Arms** (standard paired ruler vs the reactive RoR pair
`ppo:depot:b0k/b0k_sX.zip,depot:ldraft/ldraft_sX.zip`, pilots 10x10, fulls
40x25 @ 34M, fresh confirms @ 35M):

| arm | candidate battle half | full delta | 95% CI | confirm delta | 95% CI |
|---|---|---:|---|---:|---|
| A `lguard` | guard(b0k_sX) | +0.0281 | [+0.0254, +0.0307] | +0.0299 | [+0.0264, +0.0336] |
| B `ens` | mean-probs(b0k s0\|s1\|s2) | +0.0264 | [+0.0203, +0.0326] | +0.0292 | [+0.0238, +0.0349] |
| **C `lens`** | guard(ens) | **+0.0572** | [+0.0514, +0.0630] | **+0.0568** | [+0.0509, +0.0626] |

**All three arms are CI-positive on BOTH anchor ranges; C clears the
pre-registered +0.03 HEADROOM bar on both** — the first reactive-rung
headroom verdict since E18b's learned draft, and the first ever from pure
play-time machinery (no training, no new artifacts: the recipe is a spec
over existing depot blobs). Absolute avg-hard3: 0.856 / 0.847 across the
two ranges, vs the RoR's 0.798 / 0.790.

**Findings:**

1. **The two mechanisms are disjoint and compose additively.** A (+0.028)
   + B (+0.026) ≈ C (+0.057) on both ranges. The guard fixes terminal
   conversion; the ensemble de-noises the argmax everywhere else — no
   overlap to saturate, unlike every training-side diversity pair (E13).
2. **E14a's 18.3% missed-lethal pathology converts to ~+0.03 win rate.**
   Mechanism probe (serial, 400 games vs HARD3+boardkeep @ 36M): the guard
   fires 0.81x/game and CHANGES the net's move 92% of the times it fires —
   almost every found lethal is one the net was about to miss, matching
   E14a's shadow measurement almost exactly (it predicted ~0.8
   available lethals/game with ~18% missed). Boardkeep cell: 0.60 -> 0.67.
3. **Cost is negligible.** Mean 675 nodes/search, 13% cap-hit rate at
   node_cap=3000, guarded 0.26 s/game vs plain 0.11 (2.5x, still ~3x
   cheaper than the vbeam planner and ~25x cheaper than rbeam).
4. **The policy-head ensemble is the fourth policy/value dissociation,
   inverted.** E8/E12 established critic ensembling as a planner lever;
   this shows the same three checkpoints ALSO carry +0.03 of pure
   policy-head noise at 3x inference cost — the reactive rung was partly
   argmax noise, not only capability.

**Stage E (edraft full confirm — E20's open item): edraft does NOT match
ldraft at full scale.** Reactive `ppo:b0k_sX,edraft` vs the RoR pair:
**-0.0188** [-0.0278, -0.0101]; planner `vbeam:<shared ens>,8,20,edraft`
vs the 0.978 RoR: **-0.0050** [-0.0098, -0.0004]. Both CI-negative — the
E20 pilot's "matches the net drafter under the planner" read does not
survive 40x25 (pilot CIs were simply wide). Corrected standing: the
zero-inference heuristic retains ~85% of ldraft's reactive gain and ~92%
of its planner gain; keep `depot:edraft` as the no-net deployment option,
do NOT treat it as equivalent. E20's promotion question is answered: no.

**PROMOTED (pre-registered gates: full AND confirm ci_lo > 0, headroom
bar +0.03 met by C).** The reactive-rung recipe of record is now

    lppo:depot:b0k/b0k_s0.zip|depot:b0k/b0k_s1.zip|depot:b0k/b0k_s2.zip,depot:ldraft/ldraft_sX.zip

(avg-hard3 ~0.85, ~0.26 s/game), superseding `ppo:depot:b0k/b0k_sX.zip,
depot:ldraft/ldraft_sX.zip` (0.791). Honest labeling: `lens` is "guarded
reactive" — it performs an exact own-turn solve, so the rung is no longer
purely reactive, but it remains net-inference-bounded, fair, and in the
same deployment cost class (well under the planner's 0.7 s/game). For
strictly-1x-inference deployments, arm A (`lppo:b0k_sX,ldraft_sX`,
+0.030 at +0.15 s/game) is the drop-in upgrade; the three-seeds-become-
one-artifact caveat from E8 now applies to the reactive rung too.
Boardkeep guard for the promoted recipe (E10/E18c protocol, 2000 mirrored
games @ 5M common random numbers): **0.306** [0.286, 0.326] — the exploit
falls further below E18c's 0.408 vs the unguarded pair; guard-rail PASS
(`guard_boardkeep_lens` in `runs/e26-summary.json`).

**Read.** The reactive rung had ~+0.06 of pure play-time headroom sitting
on top of frozen checkpoints — exactly the two pathologies E14a measured
(missed lethals, noisy first-action argmax), each closed by the cheapest
possible exact-or-averaging machinery rather than training. Open items
recorded, not scheduled: lguard under the planner/rbeam rungs (vbeam's
width-8 beam can in principle prune a lethal the exhaustive solver would
catch — worth a cheap head-to-head probe); an anti-lethal (defensive)
guard is NOT justified by current diagnostics (E14a H2 found pass-timing/
overextension near zero).
## Net-probe instrument: NN-utilization metrics before the arch sweep (2026-07-18, branch `feat/net-probe`)

Built the measurement layer the architecture experiments need: per-layer
activation spectra (participation ratio, effective rank, n90/n99), unit
health (tanh saturation / relu death), linear CKA (trained-vs-reinit,
layer-vs-layer), and ridge linear probes (outcome AUC, masked teacher-action
accuracy) — every probe reported against the SAME probe on the raw
observation, the control that separates "the layer computes" from "the layer
re-embeds the input". `locma/stats/netdiag.py` (numpy-pure, CI-safe) +
`locma/stats/activations.py` (MaskablePPO capture, flat towers + token
extractor internals) + `scripts/net_probe.py` driver; 23 tests.

Pilot on existing practicums (states are teacher-vs-baseline, caveat):
ab-flat-s0 and depot:b0k both show the SAME picture — policy towers use
~11-16 effective dims of 64, value towers collapse to PR 2-3 (init ~13),
first-layer tanh saturation is heavy (flat pi_a1 97% of units — the
unnormalized obs; token vf_a1 88%), and NO hidden layer decodes the
teacher's action better than a linear probe on the raw observation (b0k on
dmcts data: best layer 0.382 vs raw 0.377; the model's own argmax 0.388).
Preliminary verdict: no capacity pressure — width/depth unlikely to be the
sweep's lever; conditioning (normalization) and the head/objective are the
live candidates, and the trunk findings again point at search-at-play-time.
Held-out netdmcts-teacher probe data + seed replication handed off to the
fast box: `docs/net-probe-HANDOFF.md`.

## Net-probe VERDICT: held-out replication — capacity is NOT the lever; conditioning + head are the only live arch arms (2026-07-18, branch `feat/net-probe`)

Ran the handoff protocol on this box (the "fast box" leg): recorded HELD-OUT
probe practicums at seed 1,000,000 (outside every training stream) and
replicated the probe matrix over all three b0k seeds.

Data. netdmcts teacher of record (`netdmcts:1,320,1.5,depot:shared/
shared_s0.zip,depot:ldraft/ldraft_s0.zip` — NB the handoff's literal
`--teacher netdmcts` would have hit the registry defaults K=15,I=80 and a
nonexistent `model.zip`; used the promoted E23 spec instead), 4 opponent
shards x 50 games x 2 seats = 400 games, 9,269 states, merged with game_id
offsets. Companion `mcts:100` sets: 150 games, 33,186 states each in token
AND flat encodings. Probe matrix: b0k s0/s1/s2 x {netdmcts, mcts-token} +
ab-flat-s0 on mcts-flat; JSONs committed under `runs/netprobe/` (gitignore
carve-out).

Held-out picture = pilot picture, on every axis, stable across all 3 seeds:

- **Spectra.** Policy towers use 10-18 effective dims of 64 (PR/w 0.15-0.28);
  value towers collapse to PR 2.1-5.1 (init ~13); token `slots` 92-112 of
  1280 (0.08); features 22-30 of 256. Nothing approaches the PR/w > 0.5
  capacity-bound criterion.
- **Saturation.** flat pi_a1 94% of tanh units saturated (unnormalized 308-d
  obs), token vf_a1 67-88% — but always WITH low PR, i.e. a conditioning
  problem, not capacity pressure.
- **Action probes.** No layer beats the raw-obs probe on teacher-action
  accuracy anywhere in the matrix. netdmcts set: raw 0.607, best layer
  (slots) 0.575-0.582, every layer downstream loses tactical information.
  mcts-token: raw 0.345, slots 0.343-0.353 (+0.008 max — under the ~0.01
  noise floor), all tower layers <= 0.316. The >= +0.03 criterion for
  "trunk computes features the head wastes" is not met by any cell.
- **Sharpest new fact (held-out only).** A ridge probe on the RAW observation
  predicts the netdmcts teacher's move better (0.607) than the full trained
  b0k net's own masked argmax (0.517/0.544/0.548 for s0/s1/s2). The trained
  trunk is not even the best *linear* user of its input for tactical
  matching. Consistent with the ~0.37 BC-agreement cap (2026-06-27) and the
  E22/E23/E24 line: the representational content plateaus, search at play
  time is what converts it into decisions.
- **The critic still genuinely computes.** mcts-token: vf AUC 0.92-0.93 vs
  raw 0.882; flat: 0.93 vs 0.881 — a real low-dim value feature (PR ~2-3).
  (The netdmcts set is uninformative for outcome AUC: the teacher wins
  ~everything, majority baseline 0.996.)

VERDICT against the decision criteria: the capacity-bound branch is dead.
An arch sweep, if run at all, should test ONLY (a) input conditioning —
obs normalization / LayerNorm placement, motivated by the near-total
first-layer saturation — and (b) head/objective work. Width/depth variants
of the towers are not justified by any measurement. The trunk plateau is
representational content, not capacity; play-time search (rbeam RoR)
remains the lever.

Caveats: probe states are teacher-vs-baseline, off-distribution vs b0k's own
play (netdmcts states are also easier — masked-majority 0.490 vs 0.311 on
mcts sets); b0k's higher agreement with netdmcts (0.52-0.55 vs 0.36-0.37
with mcts:100) partly reflects the shared-family critic guiding that search.
Probe deltas < 0.01 are noise per the seed spread.

## 2026-07-19 — E27: concept probes — the net computes exactly ONE concept beyond its input (winning side); missed lethals are a READOUT failure, item underuse is a REPRESENTATION failure

Question. The prestudy (PR #83) showed no layer beats the raw observation at
decoding the teacher's action. E27 asks the finer, mechanism-level question
per concept: when the net plays badly around a concept (18% missed lethals,
3.3x item underuse, threat blindness — E14a), is the concept MISSING from
the representation, or PRESENT but unused by the head?

Method. New `labeler` hook in `record_practicum` captures ground-truth
concept labels from the FULL game state at every teacher decision
(`locma/stats/concepts.py`); `scripts/e27_concept_probe.py` records and
probes. Labels: `lethal_now` (exhaustive `lguard.find_lethal` DFS from E26 —
17.7% positive, 1.3% cap-hits excluded), `opp_threat_lethal` (board-vs-guard
threat arithmetic, 6.5%), `winner_side` (mover's seat won the game),
`item_now` (teacher plays an item, probed where an item is legal),
`can_item`, plus near-linear controls (hp_diff, own_mana, board_atk_diff,
my_guards). 33,186 held-out states (seed 1M, mcts:100 teacher, token obs);
ridge probes per layer vs the SAME probe on the raw obs; game-level split;
shuffled-label controls all ~0.50; b0k s0/s1/s2 replication (all numbers
below stable across seeds).

Results (s0; AUC for binary, R^2 for continuous):

| concept           |   raw | slots | features | pi_a1 | pi_a2 | vf_a1 | vf_a2 | best-raw |
|-------------------|------:|------:|---------:|------:|------:|------:|------:|---------:|
| lethal_now        | 0.986 | 0.875 |    0.981 | 0.954 | 0.947 | 0.962 | 0.963 |   -0.005 |
| opp_threat_lethal | 0.977 | 0.866 |    0.978 | 0.934 | 0.905 | 0.954 | 0.949 |   +0.001 |
| winner_side       | 0.882 | 0.808 |    0.912 | 0.907 | 0.893 | 0.931 | 0.931 |   +0.050 |
| item_now          | 0.669 | 0.572 |    0.639 | 0.601 | 0.590 | 0.582 | 0.578 |   -0.030 |
| can_item          | 0.983 | 0.929 |    0.761 | 0.661 | 0.619 | 0.612 | 0.608 |   -0.055 |
| hp_diff (R^2)     | 1.000 | 0.173 |    0.794 | 0.585 | 0.566 | 0.606 | 0.587 |   -0.206 |
| own_mana (R^2)    | 1.000 | 0.421 |    0.627 | 0.313 | 0.255 | 0.170 | 0.167 |   -0.373 |
| my_guards (R^2)   | 1.000 | 0.796 |    0.332 | 0.147 | 0.110 | 0.106 | 0.112 |   -0.204 |

Findings.

1. **`winner_side` is the only concept the trunk computes beyond its input**
   — vf tower 0.920-0.931 vs raw 0.882, +0.041..+0.050 across seeds, the
   ONE case clearing the prestudy's +0.03 criterion. The critic's low-dim
   computed feature now has a name: who is winning.
2. **Missed lethals are a READOUT failure, not representational.**
   `lethal_now` is nearly linearly decodable from the raw obs (0.986 — the
   mcts-state lethal is mostly simple attack arithmetic) and still ~0.95-0.96
   decodable at tower depth; yet the policy misses 18% of them in play. The
   information survives to the last layer; the head does not act on it.
   E26's lens guard is the correct fix; more trunk cannot help.
3. **Same for reply threat**: `opp_threat_lethal` 0.977 raw, net never
   better. rbeam's edge is not about *representing* threat — it is about
   *resolving* it by search.
4. **Item underuse has a representational mechanism**: `can_item` decays
   monotonically down the trunk, 0.983 (raw) -> 0.93 (slots) -> 0.76
   (features) -> ~0.61 (towers). The trunk progressively DISCARDS
   hand-item information, so by decision level the policy barely knows it
   holds a playable item; `item_now` is weak even on raw (0.669, n_test 603
   — noisy) and worse everywhere in the net. First concrete mechanism for
   the 3.3x underuse; points at the conditioning arm (or a hand->head skip
   path) rather than width, and at play-time item nudges as the cheap fix.
5. Controls behave: raw R^2 = 1.000 exactly (labels are linear in obs — the
   pipeline is honest); guards/mana are heavily compressed by depth.

Caveats. The raw obs probe has 373 linear dims vs 64 in a tower — deltas vs
raw are conservative for "adds beyond obs", so read ABSOLUTE retention for
the present-but-unused claims (0.96 lethal AUC at the last layer stands on
its own). lethal/threat sit near ceiling on raw, so +0.03 is unreachable
there by construction; the informative readout is that depth only ever
LOSES them. `item_now` n is small (items legal in 8.9% of states).

VERDICT. Representation is NOT the binding constraint for lethal/threat —
the head/readout is (play-time guards + head/objective work correctly
targeted). Representation IS partially binding for items (information
actively discarded with depth — conditioning arm / skip paths). The value
head computes exactly the concept the planner exploits. Capacity stays dead.

## 2026-07-19 — E28 gate 1: pointer action head BREAKS the BC-agreement cap (+0.04, capacity-controlled) — but not via items

First arm of the reactive-limits program (docs/reactive-limits-program.md).
Hypothesis under test: the standard dense action head (`action_net`, Linear
64->155) is the bottleneck E27 located — per-card information must squeeze
through the 64-d tower latent to reach "its" logit; a pointer-style head that
computes each action's logit FROM the slot tokens of the cards it involves
should restore item behavior and match BC agreement.

Setup (`scripts/e28_pointer_bc.py`): all arms init from depot:b0k/b0k_s0,
token extractor FROZEN (identical features — isolates the head), pi tower +
head fine-tuned by masked-CE behavior cloning on the labeled held-out
practicum (runs/e27-concepts.npz, mcts:100 teacher, seed-1M, game-level
80/20 split), 12 epochs, seeds 0/1. PointerHead: logit(a) =
MLP([z_src(a), z_tgt(a), tower_ctx, family_onehot]), shared 128-hidden MLP,
~25k params. Capacity-matched control: fresh dense MLP head 64->113->155
(same ~25k head params, same total 45.6k trainable vs pointer 45.9k).

Validation results (seed 0 / seed 1; all arms converged, flat last-3):

| arm                        | trainable | agreement     | item recall   | lethal-state agree |
|----------------------------|----------:|---------------|---------------|--------------------|
| b0k as-is (reference)      |         — | 0.370 / 0.367 | 0.249 / 0.194 | 0.219 / 0.222      |
| control (warm dense head)  |     30.7k | 0.390 / 0.390 | 0.283 / 0.241 | 0.220 / 0.217      |
| control_mlp (fresh, matched)|    45.6k | 0.391 / 0.392 | 0.295 / 0.282 | 0.217 / 0.222      |
| **pointer (fresh)**        |     45.9k | **0.423 / 0.436** | 0.289 / 0.277 | 0.221 / 0.256  |

Findings:

1. **Gate criterion 1 PASSED, decisively and capacity-controlled.** The
   pointer head clears both controls by +0.032..+0.044 on both seeds at
   matched trainable params. This is the first architecture change to break
   the ~0.37-0.39 BC-agreement plateau against a search teacher (the cap
   that held through E4v2/E15 and the 2026-06-27 distill study). The
   capacity-matched control shows head WIDTH does nothing (0.391 ~= 0.390):
   the gain is structural slot access, full stop.
2. **The item hypothesis was WRONG in its specifics.** Item recall is a wash
   vs the matched control (0.289/0.277 vs 0.295/0.282); item-state agreement
   no better. Giving the decision direct access to hand-card tokens does NOT
   fix item choice under BC — extending E27's "representing != using" lesson
   to the head itself: item underuse is not an information-ACCESS problem
   anywhere in the net. Most plausible residual: item plays require valuing
   consequences (E14a turn-level branching), i.e. computation, not routing.
3. Where the +0.04 comes from instead: broad decision quality (the gain is
   NOT concentrated in item or lethal states — those conditional agreements
   barely move). Consistent with slot-specific targeting (which attacker,
   which summon) benefiting from direct slot reads.

Gate verdict: **PASS — proceed to gate 2** (PPO retrain at the b0k recipe
with the pointer head, paired ruler vs ppo:b0k,ldraft + boardkeep
guard-rail). The BC gain justifies the training spend on its own; whether
PPO converts slot access into item usage where BC could not is exactly what
gate 2 measures. Artifacts: runs/netprobe/e28_gate1_*.json.

## 2026-07-19 — E28 gate 2: pointer-head PPO retrain — HEADROOM +0.073, replicated on fresh anchors; first architecture win, pure net reaches the guarded-RoR band

Gate 2 of the reactive-limits program: two pointer-head seeds trained at the
EXACT b0k recipe (token V0, lr=1e-4, target_kl=0.025, dropout=0.1,
n_envs=16, 5-phase zoo x 200k = 1M steps; `locma train-zoo --pointer-head`,
new `PointerMaskablePolicy` in locma/envs/pointer_head.py), benched by the
standard paired ruler vs the reactive RoR pair `ppo:b0k_sX,ldraft_sX`
(driver: scripts/e28_gate2_bench.py, summary in
runs/netprobe/e28_gate2_summary.json).

| stage | result |
|---|---|
| full 40x25 @ 40M | **+0.0729 [+0.0652, +0.0808] — HEADROOM** (cand 0.8564 vs b0k 0.7835) |
| confirm 40x25 @ 41M (fresh) | **+0.0687 [+0.0608, +0.0765] — HEADROOM** (0.8579 vs 0.7893) |
| boardkeep guard-rail (2000 mirrored @ 5M CRN) | boardkeep wins **0.258 / 0.310** vs e28p s0/s1 — vs 0.408 against b0k: MORE robust, no hole |
| item rate per opportunity (60 self-teacher games @ 42M) | e28p 0.144 / 0.214 vs b0k 0.171 — seed-dependent, not the mechanism |

Pre-registered gates: **gate2_pass = TRUE** (both CIs positive), **headroom
= TRUE** (+0.073 >= +0.03). Both far above every bar.

Readings:

1. **First architecture/training win after seven training-side nulls** — and
   the largest single reactive-rung gain in the project's history (+0.073;
   E18b's draft swap was the previous best training-side jump, E26's lens is
   +0.057 from play-time machinery). The BC-cap break in gate 1 predicted
   real play strength: the dense action head was a genuine bottleneck.
2. **A pure net now sits in the guarded-RoR band**: e28p avg-hard3
   0.856/0.858 vs the lens-guarded recipe's ~0.85 (0.856/0.847, different
   anchor ranges — comparable band, not a paired claim). Program milestone 1
   ("pure net > 0.85") is effectively reached pending a direct pairing.
3. **The boardkeep result is a second surprise**: the pointer nets nearly
   neutralize the archetype that has resisted everything since E10
   (0.26-0.31 vs the 0.5 parity bar b0k never cleared from below; E11's
   zoo hardening only got 0.512). Slot-addressed logits appear to fix the
   disciplined-trader weakness — plausibly the same targeting-precision
   mechanism as the gate-1 agreement gain.
4. **Items remain unmoved** (seed-split read straddles b0k) — three
   independent instruments now agree (E27 probes, gate-1 BC, gate-2 PPO):
   item underuse is not access, and not PPO-trainability of access; it is
   consequence valuation (E14a branching). E30 territory.

Next (not yet run): seed s2 for the standard 3-seed artifact; direct paired
ruler vs the lens-guarded recipe; lens-over-pointer stack (`lppo:` accepts
any checkpoint list) as a cheap possible ~0.9; depot publish + promotion
decision.

## 2026-07-19 — E28 stack: 3-seed artifact confirmed + lens-over-pointer at 0.908 BEATS the guarded RoR — new reactive summit

Follow-ups to gate 2: seed s2 trained at the same recipe, then four
pre-registered 40x25 verdicts @ fresh 43M anchors + a 44M confirm
(driver scripts/e28_stack_bench.py, summary
runs/netprobe/e28_stack_summary.json).

| stage | result |
|---|---|
| pure3: pointer trio vs b0k trio | **+0.0714 [+0.0646, +0.0785]** (0.8653 vs 0.7939) — gate 2 holds at 3 seeds |
| stack vs guarded RoR: `lppo:e28p trio` vs `lppo:b0k trio` | **+0.0593 [+0.0531, +0.0658]** (**0.908** vs 0.8487) — HEADROOM over the recipe of record |
| lens increment on pointer nets | **+0.0418 [+0.0366, +0.0471]** (0.9071 vs 0.8653) — the guard still adds |
| boardkeep guard-rail (2000 mirrored @ 5M CRN) | boardkeep wins **0.221** vs the stack (0.306 vs current guarded RoR, 0.408 vs unguarded b0k) |
| confirm @ 44M (fresh) | **+0.0583 [+0.0524, +0.0642]** — replicated; `stack_beats_ror: TRUE` |

Readings:

1. **New reactive summit: 0.908 avg-hard3** — above the single-critic vbeam
   planner ceiling (0.890) and within 0.02 of the 3-critic ensemble planner
   RoR (0.926), from a reactive net + the cheap lethal guard (~0.26 s/game,
   ~3x cheaper than vbeam, ~25x cheaper than rbeam).
2. **The lens mechanisms stay disjoint under the pointer head**: +0.042
   increment vs E26's +0.057 on b0k — slightly smaller (the pointer net
   finds more kills itself) but decisively positive: exhaustive lethal
   search and slot-addressed logits fix different failures.
3. **Program milestones**: pure trio 0.865 clears milestone 1 (>0.85 without
   guards, now at 3 seeds); milestone 2 (pure net ~0.890) is 0.025 away —
   E29 (conditioning) and E30 (turn-plan head) are the remaining levers.
4. Boardkeep 0.221 vs the stack is the most exploit-robust configuration
   ever measured on this project.

PROMOTION CANDIDATE (both rungs, pending decision): reactive recipe
`ppo:e28p_sX,ldraft_sX` (0.8653) and guarded-reactive recipe
`lppo:e28p s0|s1|s2,ldraft_sX` (0.908) — requires depot publish of the
three checkpoints (provenance: train-zoo --pointer-head, b0k recipe,
seeds 0/1/2) and the baseline.md recipes-of-record update.

## 2026-07-19 — E28 promotion: depot:e28p published; pointer head is the recipe of record on BOTH reactive rungs

Promotion executed (decision follows directly from the stack-bench entry
above — every pre-registered gate cleared with headroom, full + fresh-anchor
confirm on each):

- **`depot:e28p` v1 published and pushed** (gh:depot/e28p-v1, parent b0k@1):
  `e28p_s{0,1,2}.zip`, provenance = `locma train-zoo --pointer-head` at the
  exact b0k recipe, meta carries the headline numbers.
- **Reactive recipe of record**: `ppo:depot:e28p/e28p_sX.zip,
  depot:ldraft/ldraft_sX.zip` — **0.865** (was b0k's 0.791, +0.073/+0.069
  gate-2 fulls; +0.071 at 3 seeds).
- **Guarded-reactive recipe of record**: `lppo:depot:e28p/e28p_s0.zip|
  depot:e28p/e28p_s1.zip|depot:e28p/e28p_s2.zip,depot:ldraft/ldraft_sX.zip`
  — **0.908** (was the b0k lens's ~0.85; +0.059 confirmed +0.058).
- baseline.md gained the 2026-07-19 recipes-of-record section (header
  pointer updated); reactive-limits-program.md E28 arm marked PROMOTED,
  milestone 1 (pure net > 0.85) marked reached.

Notable: the single pointer net at 0.856-0.858 already beats the prior
GUARDED record — strictly-1x-inference deployments upgrade too. E26's lens
stays in the guarded recipe (its increment on pointer nets is +0.042,
mechanisms disjoint). Program continues at E29 (conditioned trunk) / E30
(turn-plan head); milestone 2 is the pure-net 0.890 bar, 0.025 away.

## 2026-07-19 — E28b: mixing ablation — the pointer gather does NOT need the transformer

Follow-up gate before E29, prompted by the encoder introspection
(docs/notes/e28-encoder-viz.html: attention is near-uniform pooling, the
card-id embedding never left init). Question: is the transformer's
cross-card mixing load-bearing for the pointer head's gather, or is
slot-structured ACCESS the whole mechanism? Gate-1 protocol with one
variable changed (scripts/e28b_mix_ablation.py, frozen b0k_s0 extractor,
trainable pi-tower clone + fresh pointer head, masked-CE BC on the e27
practicum, 12 epochs, seeds 0/1; context latent_pi identical in every arm —
only the gathered per-slot representation varies):

| arm (gathered rep)                        | s0 agree | s1 agree |
|-------------------------------------------|---------:|---------:|
| pointer — post-attention z (gate-1 ref)   | 0.4229   | 0.4355   |
| premix — x0 = proj+LN+pos, BEFORE mixing  | 0.4255   | 0.4315   |
| raw — [feats\|id_embed] 33-d, no proj     | 0.4289   | 0.4302   |

premix − pointer = +0.0026 / −0.0040 — pre-registered verdict
**MIXING_UNNECESSARY** (within 0.005 both seeds). Even the RAW arm
(bypassing the learned projection entirely, capacity-matched hidden 186)
matches. Item recall is no worse without mixing (0.36 vs 0.29 at the end,
noisy metric); pointer s0 reproduces gate-1's 0.423 exactly.

Readings:

1. **The BC-cap break is 100% structural slot access.** The transformer
   contributes nothing measurable to the gathered tokens — the mechanistic
   counterpart of the encoder-viz nulls (diffuse attention, untrained id
   embedding, which the raw arm carries only as a frozen random hash).
2. **Scope caveat**: latent_pi still flows through the full frozen trunk in
   all arms. This gate clears the GATHER path only; dropping the
   transformer from the whole trunk (context + critic) is a retrain
   question — but it is now a justified arm, worth folding into E29:
   the conditioning sweep should include a slim/transformer-free extractor
   variant (the reactive rung's selling point is cheapness; the extractor
   was ~4x the flat net's compute at introduction).
3. Not tested in isolation: dropping id_embed from the raw gather (it is
   ~random init, probes at chance) — trivial rider on the next arm if it
   matters.

Artifacts: runs/netprobe/e28b_mix_summary.json.

## 2026-07-20 — E29 slim stack: PROMOTED both rungs — 0.903 pure / 0.934 guarded, the guarded stack now BEATS the ensemble planner

slim_s2 trained, completing the 3-seed artifact; scripts/e29_slim_stack_bench.py
mirrored the e28c promotion ladder with slim as candidate, the promoted e28c
artifact as incumbent (fresh anchors 61M/62M; guard seed 5M CRN):

| stage | result |
|---|---|
| pure trio vs e28c trio @ 61M | **+0.0224 [+0.0158, +0.0294]** (0.9026 vs 0.8801) |
| **stack: `lppo:slim trio` vs the 0.914 guarded RoR @ 61M** | **+0.0207 [+0.0163, +0.0251]** (0.9344 vs 0.9137) |
| stack confirm @ 62M (fresh) | **+0.0210 [+0.0159, +0.0262]** (0.9382 vs 0.9172) |
| lens increment ON slim nets @ 61M | +0.0318 [+0.0263, +0.0374] — headroom, still disjoint |
| boardkeep vs the slim stack (2000 mirrored @ 5M CRN) | **0.189** — beats e28c's 0.2185, most exploit-robust yet |

Every promotion-relevant CI excludes zero with a fresh-anchor replication —
the E7 precedent again. **PROMOTED on both rungs.** New records: reactive
**0.903** (was 0.878), guarded **0.934** (was 0.914). Two milestones:
- pure 0.903 clears milestone 2 (pure net ~0.890, the single-critic planner
  ceiling) with NO guards;
- guarded 0.934 SURPASSES milestone 3's target — the 0.926 ensemble-planner
  RoR — so the guarded reactive stack now beats the planner outright, at a
  fraction of its cost AND with a 7.4x-cheaper extractor.

The win came from REMOVING capacity (the transformer), not adding it —
consistent with E28b (mixing unnecessary) + the encoder-viz nulls. Published
`depot:e29slim` v1 (parent e28c@1). baseline.md RoR section updated; e28c
recipes stand as the prior records.

Next: the E29/E30 arch-sweep is done — E29a conditioning closed-negative, E30
factorization closed, E29 slim promoted. The reactive net has now internalized
everything the single-beam planner had; remaining net-vs-search gap is
lookahead depth, which play-time search (rbeam RoR) supplies.

Artifacts: runs/e29-slim-stack-summary.json.

## 2026-07-20 — E30: turn-plan BC diagnostic — the ~0.37 cap is REPRESENTATIONAL, not factorization; arm CLOSES

E30's hypothesis (docs/reactive-limits-program.md): the ~0.37 per-micro-
action BC-agreement cap is a FACTORIZATION artifact — a head predicting each
action independently can't represent a multi-step turn plan ("starts lines,
can't finish them", E14a). Test: does an AUTOREGRESSIVE head that also sees
the plan so far clear the cap?

Controlled diagnostic (scripts/e30_plan_bc.py; one variable): both arms share
identical frozen state features (raw token obs + scalars) and identical BC
training on a search-teacher token practicum (mcts:100, n=35150). Turns are
reconstructed by (game_id, round counter = obs_scalars[:,0]) — NOT Pass,
which mis-delimits (only ~4.5% of decisions are Pass; the correct split gives
12965 turns, mean 2.7 actions). Arms differ ONLY in plan conditioning:
  - factored: logits = MLP(state)
  - autoreg:  logits = MLP([state, GRU(actions taken earlier this turn)]),
              teacher-forced.

| arm | multi-action agreement | overall | whole-turn exact |
|---|---|---|---|
| factored | **0.373** (reproduces the cap) | 0.402 | 0.247 |
| autoreg  | **0.366** | 0.395 | 0.250 |

**autoreg - factored on multi-action turns = -0.008 / -0.005 / -0.004
(seeds 0/1/2) — flat-to-slightly-NEGATIVE, all three seeds.** Pre-registered
rule (delta <= +0.03 -> representational): E30 CLOSES.

Reading: explicit plan-so-far conditioning adds NOTHING beyond the state —
because the board state already updates after each action, so it encodes
"what I've done this turn". The ~0.37 cap is representational, not a
factorization failure: the reactive obs/features do not separably encode
WHICH action a lookahead teacher picks (the search does branching the single
forward pass cannot). This is the "else closes cleanly" branch of the E30
gate. Caveat: one plan encoding (GRU over action indices) + one teacher
(cheating mcts:100, the hardest-to-imitate); but the natural autoregressive
signal being redundant with the state is a strong, seed-stable negative.

Consequence for the program: BOTH remaining training-/arch-side item/plan
levers are now closed (E29a conditioning negative, E30 factorization
representational). The surprise WIN was a SIMPLIFICATION (E29 slim, transformer
dropped); play-time search (rbeam RoR) remains what fills the lookahead gap.

Artifacts: runs/netprobe/e30_plan_bc.json (+ _s1/_s2).

## 2026-07-20 — E29 slim extractor: dropping the transformer BEATS e28c (+0.026) at 7.4x fewer extractor params — promotion candidate

The E29 sibling to the (closed) conditioning arm, a separate hypothesis:
cheapness, not conditioning. E28b showed the transformer's cross-card
mixing is unnecessary for the pointer gather, and the encoder introspection
found attention near-uniform + the id embedding untrained — so is the
transformer earning its compute at all? SlimTokenExtractor drops it: per-slot
embeddings (proj + LayerNorm + positional, via a hookable SlotEncoder that
the pointer head gathers) + cheap masked mean/max pooled context + scalar
branch. 56.7k extractor params vs 418.5k (7.4x fewer); the pointer policy
hooks slot_encoder or transformer, whichever the extractor exposes.

slim_s{0,1} = `train-zoo --pointer-head --obs-mode token-fx --slim-extractor`
at the EXACT e28c recipe. Bench (scripts/e29_slim_bench.py) vs the e28c RoR
pair — pre-registered as a MATCH test (cheapness), success = not a
regression:

| stage | result |
|---|---|
| full paired ruler @ 58M | **+0.0259 [+0.0198, +0.0320]** (0.903 vs 0.877) — slim WINS |
| confirm @ 59M (fresh) | **+0.0282 [+0.0208, +0.0354]** (0.902 vs 0.874) — replicated |
| extractor params | slim 56.7k vs full 418.5k = 7.38x fewer |
| wall-clock s/game | slim 0.0795 vs e28c 0.0933 = 1.17x faster (engine + towers dominate end-to-end) |
| boardkeep guard-rail | 0.236 / 0.247 — in the e28c band, no hole |

**Not just parity — a strength WIN, replicated (E7 pattern).** Removing the
transformer improved win rate AND cut extractor compute 7.4x. Reading:
the near-uniform attention + untrained id embedding (encoder-viz) mean the
transformer was adding capacity that overfit rather than signal; the slim
per-slot encoder is a regularizing simplification. Sub-headroom (+0.026 <
+0.03) but zero-excluding twice. Promotion candidate on both rungs — s2
training now, then the stack ladder (lppo:slim trio vs the 0.914 e28c
guarded RoR) decides, mirroring the e28c promotion flow.

Artifacts: runs/e29-slim-summary.json.

## 2026-07-20 — E29a: conditioned trunk (feature_ln) — the saturation fix WORKS but is DECOUPLED from the item info-loss, and is CI-NEGATIVE on win rate

The E29 premise (docs/reactive-limits-program.md): the net-probe prestudy
found the SB3 tower's first Tanh layer ~94% saturated, hypothesized as the
MECHANISM behind E27's can_item info-loss (0.98 raw -> 0.61 towers), so
conditioning the trunk should be a multiplier on every historical
frozen-/handicapped-trunk null. Lever: opt-in `feature_ln` — LayerNorm on
the extractor's fused output (the tower input), `TokenSetExtractor(
feature_ln=True)` / `train-zoo --feature-ln`, default OFF/byte-identical.

Gate (scripts/e29_gate_probe.py): conditioned vs matched control, both
trained to a REDUCED 500k budget on the exact e28c recipe (token-fx +
pointer, seed 0), probed on a frozen deploy set (n=9142). Pre-registered:
saturation must DROP and can_item tower-retention must IMPROVE.

| read | control | conditioned |
|---|---|---|
| pi_a1 first-Tanh saturation | 0.566 | **0.258** (halved) |
| vf_a1 first-Tanh saturation | 0.787 | 0.658 |
| can_item retention @ towers | 0.745 | 0.741 (flat; raw obs 0.901) |

**Gate 1 PASS, gate 2 FAIL.** The LayerNorm demonstrably halves first-layer
saturation — but can_item retention does not move. So saturation and the
item info-loss are DECOUPLED: fixing the saturation does not restore the
information the trunk drops. That refutes the specific mechanism E29 rested
on.

Tiebreaker (can_item was only a proxy): paired win-rate ruler on the two
already-trained seed-0 nets, matched 500k, 40x25 @ 57M:
**conditioned -0.0147 [-0.0245, -0.0053] vs control** (0.872 vs 0.887) —
CI-NEGATIVE. So the saturation fix slightly HURTS play. Two independent
reads (mechanistic gate-2 + direct win rate) agree.

**Verdict: E29a CLOSED (negative).** feature_ln fixes a real but apparently
COSMETIC pathology — first-layer saturation is not the training handicap
the prestudy framing assumed; "fixing" it neither restores item info nor
helps win rate (costs a little). This weakens the broader "ill-conditioned
trunk gates the historical nulls" rationale. Caveats: one training seed,
reduced 500k budget (a full-budget flip is unlikely given gate 2 also
failed, but not excluded). The `feature_ln` lever + gate driver are kept
(clean opt-in, default-off) for reproducibility, like the E6 LSTM tooling.
Live E29 sibling still open: the slim/transformer-free extractor arm
(different motivation — cheapness + E28b's mixing-unnecessary result), a
distinct hypothesis from trunk conditioning.

Artifacts: runs/e29-gate-summary.json.

## 2026-07-20 — E28d: fx + item-rich training decks — CI-NEGATIVE; the item-conversion gain was already saturated at the item-light diet

The E28c escalation (worklog E28c reading 3): fx was CI-positive and moved
item behavior DESPITE an item-starved training diet, so "train on decks
that actually contain the cards it can now see" looked like the obvious
next lever. It is not.

First the data hole had to be closed. The E29 pre-read caveat established
that NO training deck or practicum ever contained a blue item, for two
draft-side reasons (docs/e31-draft-valuation-plan.md, filed E31): the
greedy teacher's draft score is sign-blind on items (blues score -1..-5),
and CardView hides the play-effect fields so even the magnitude-correct
`_card_value` prices 4/8 blues at ~0. E31a (scripts/e31a_value_table.py,
data-only) reads the cards DB directly and emits a DistilledDraftPolicy
values table with the true values incl. hidden effects; a +4-item-bonus
"diet" variant drafts **6.28 items/deck, 1.13 blues, 72% of decks carry
blue** (census) — the first blue-item exposure in the program. Plumbing:
`_draft_override_policy` loads values-JSON tables; `train-zoo` gains
`--draft-override`.

E28d = e28c's EXACT recipe (`--pointer-head --obs-mode token-fx`, lr 1e-4,
target-kl 0.025, n-envs 16) + `--draft-override runs/e31a_diet.json`, seeds
0/1. Bench (scripts/e28d_bench.py) vs the e28c reactive RoR pair:

| read | result |
|---|---|
| full ruler @ 50M | **-0.0260 [-0.0343, -0.0181]** (0.8445 vs 0.8705) — CI-NEGATIVE, regression |
| item rate / opp on ldraft (deploy) decks | e28d 0.224 / 0.239 — same e28c (0.226/0.194) / e28p (0.142) territory, NO lift |
| item rate / opp on diet (blue-rich) decks | e28d **0.260 / 0.240** vs **e28c 0.260** — identical |
| BLUE rate / opp on diet decks | e28d **0.192 / 0.135** vs **e28c 0.202** — NOT higher, slightly worse |

Readings:

1. **The item-conversion gain was already saturated by fx alone at the
   item-light diet.** e28c, which never trained on a blue, already plays
   items at 0.260 and blues at 0.202 per opportunity when handed blue-rich
   decks — and e28d, drenched in blues during training, does not exceed it
   on the SAME decks. More exposure added nothing to item behavior.
2. **And it cost general play** (-0.026): the diet decks are off the
   ldraft deploy distribution, so the extra item curriculum traded away
   avg-hard3 on the decks that actually ship. The distribution-shift risk
   flagged pre-bench materialized; the deploy-deck item rate (0.224/0.239)
   confirms nothing was gained to offset it.
3. **This reinforces the E27/E28 triple-agreement diagnosis and refines
   it**: item underuse is consequence valuation (E30 / turn-level
   branching), not access AND not exposure. It also weakens E31c
   (extend ldraft's draft-obs to pick blues): a battle net saturated in
   blue training doesn't convert them better, so better draft-side blue
   eyesight would not unlock battle-net item value with today's nets —
   E31c stays gated on E30, as the plan already had it.

Verdict: escalation arm CLOSED (no s2, no promotion). The E31a table and
the values-JSON `--draft-override` plumbing are correctness keepers
(independent of this null — E31's stated non-goal is any draft-side
strength claim); they land for reuse. Item lever remains E30.

Artifacts: runs/e28d-bench-summary.json, runs/e31a-values-summary.json.

### E28d addendum — are blues WEAK, or can't the net PLAY them? (blue-value diagnostic)

The E28d null admits two readings the win rate can't separate: H1 blues are
contextually weak (declining is correct, no defect) vs H2 the net can't
value them. Two net-independent reads (scripts/blue_value_diag.py, diet
decks, same draft/opponents/seed):

**1. Oracle blue-play rate.** A CHEATING perfect-information MCTS
(engine-grounded, cannot share the net's blindness) plays blues at only
**0.220 / opportunity** — and items overall at 0.302. So an optimal,
fully-informed player DECLINES ~78% of blue chances: blues are contextually
weak (H1 largely true). Against that bar: e28c (RoR) 0.170 — a modest
genuine underuse gap (~0.05); e28d (item-rich trained) 0.243 — at/above the
oracle. Item underuse on TODAY's blues is mostly correct play, not a defect.

**2. Magnitude-dose probe** (the "stronger new blues" question). Hold a real
blue-in-hand decision fixed, scale that card's fx effect columns by
k in {0,.5,1,2,3} (same card, weaker/stronger), read the fx net's own
play-probability for it (pointer Use logit for the slot). A sharp
dissociation:

| net | blue k0 -> k3 | green k0 -> k3 |
|---|---|---|
| e28c (RoR, item-light diet) | 0.201 -> 0.201 — **FLAT, magnitude-blind** | 0.233 -> 0.223 flat |
| e28d (item-rich diet) | 0.184 -> **0.234** — **monotone +0.05** | 0.202 -> 0.214 mild + |

**The E28d "null" hides a real capability gain.** Item-rich training bought
magnitude-conditioned item valuation that e28c lacks: e28d plays a card MORE
as its effect grows; e28c plays blues at a fixed ~0.20 regardless of
strength (by rote — card-type/cost, not effect). It did not convert to win
rate because with today's weak blues (oracle-confirmed) the correct policy
is to mostly decline anyway — so the new sensitivity has little to bite on,
while the off-distribution diet cost general play. Decomposition:
fx obs (E28c) = items become visible -> play rate up (COARSE); item-rich
diet (E28d) = play rate tracks effect magnitude (FINE).

Answers to the three questions: (a) mostly the cards — even a perfect-info
oracle declines most blues; (b) yes, the net can play them, and e28d plays
them roughly at the oracle rate; (c) **stronger similarly-designed blues
WOULD be played by e28d (monotone dose response) but NOT by the current RoR
e28c (flat).** So E28d is win-rate-negative on the current pool yet is the
net that would SCALE with a stronger item design — retain it as the
magnitude-conditioned artifact, not just a closed null. Also refines the
committed E28d claim: exposure DID help the representation (magnitude
reading), it just didn't help win rate on weak cards.

Artifact: runs/blue-value-diag.json.

### E28d addendum 2 — is the ORACLE wrong about blues? Yes, on card-draw (rollout-horizon test)

The oracle's "blues are weak" read rests on its leaf value
(mcts._leaf_value = health-lead + 0.5*board-power-lead) — which has NO
card-advantage term, and a 3-turn RANDOM rollout barely realizes card
draw. So the oracle is blind BY CONSTRUCTION to the value of the two
draw-carrying blues (154, 157). Per-effect split of the base oracle read
confirmed it: draw-blue rate 0.152 vs non-draw 0.234 — it declines exactly
the items it can't price; the fx net (which SEES the card_draw column)
plays draw-blues at 0.296, ~2x the oracle.

Decisive test (scripts/blue_oracle_horizon.py): same iters/seed/opponents,
extend the rollout horizon so card advantage can convert:

| oracle rollout | draw-blue rate | non-draw-blue rate |
|---|---|---|
| 3-turn heuristic (base) | 0.133 (n=60) | 0.184 (n=255) |
| 8-turn heuristic | 0.143 (n=56) | 0.207 (n=246) |
| terminal (to game end, real win/loss) | **0.258** (n=31) | 0.201 (n=289) |

Draw-blue rate nearly DOUBLES as the horizon deepens (0.133 -> 0.258,
overtaking non-draw at terminal); non-draw blues stay flat (~0.18-0.21,
the material heuristic already prices them). Exactly the mechanism-
predicted signature.

**Verified at 7x the games (scripts/blue_oracle_horizon_verify.py, fresh
seed 55M, 150 games, bootstrap-over-games 95% CIs):**

| oracle rollout | draw-blue rate | non-draw-blue rate |
|---|---|---|
| 3-turn heuristic (base) | 0.163 [0.128, 0.208], n=375 | 0.218 [0.195, 0.243], n=1618 |
| terminal (to game end) | 0.246 [0.204, 0.297], n=334 | 0.210 [0.189, 0.235], n=1958 |

The horizon-lift of draw-blues is now solid: terminal - base draw-blue =
**+0.083 [0.021, 0.143], CI excludes zero** (bootstrap). Draw-blues go from
clearly BELOW non-draw at base (0.163 vs 0.218) to PARITY-or-above at
terminal (0.246 vs 0.210) — the strict draw>non-draw crossover is
directional but the CIs overlap, so read it as "the horizon erases the
draw underuse", not "draw is played more than non-draw". Non-draw stays
flat with tight CIs (0.218 -> 0.210), confirming the effect is
draw-specific (the material heuristic already prices removal/heal/burn).

So the "blues are contextually weak" conclusion SPLITS: it holds for the 6
non-draw blues (removal/heal/burn — the oracle evaluates them soundly, rate
horizon-invariant), but is OVERTURNED for the 2 card-draw blues — the
oracle undervalued them, and the fx net playing them at ~0.30 looks
CORRECT, not a mispricing. Caveat: terminal rollout is still random play,
under-realizing optimal card-advantage, so true draw-blue value may be
higher still. General lesson: the cheating-MCTS oracle has a known blind
spot (card advantage) — don't treat
it as ground truth for draw/tempo items.

Artifact: runs/blue-oracle-horizon.json.

## 2026-07-19 — E28c: feature completion (fx obs) — CI-positive over the e28p RoR, and items MOVE for the first time

The census behind the arm: **44/160 cards — 7/8 blue items — carry play
effects (player_hp/enemy_hp/card_draw) that are NOT in the 17 numeric token
features**; the only path to them is the card-id embedding, which never
trains (E28b/encoder-viz). The net was blind to burn/heal/draw semantics.
Fix: opt-in token variant `fx` (obs_mode=token-fx, TOKEN_FEATS_FX=20) — 3
effect columns for hand cards, board slots zero; extractor reads token
width from the obs space; play-time consumers detect the variant via new
`encode.token_variant_for_space` (scalar width alone no longer suffices —
that silent-crash path is fixed and tested).

BC gate (scripts/e28c_fx_bc.py): NULL and uninformative by construction —
the e27 practicum contains ZERO blue items in any hand across 33k
decisions (804/822 teacher item plays are green; 4/24 green items have
effects), so the fx columns were ~all zeros on that data. Deeper finding:
the zoo training decks are essentially blue-item-free while deploy pairs
with ldraft decks carrying 3.9-5.2 items — a train/deploy distribution gap
on exactly the item axis (E19's null retrain was dense-head, pre-fx).

Bench (scripts/e28c_bench.py; e28c_s{0,1} = `train-zoo --pointer-head
--obs-mode token-fx` at the exact e28p recipe; paired ruler vs the CURRENT
reactive RoR pair `ppo:e28p_sX,ldraft_sX`):

| stage | result |
|---|---|
| full 40x25 @ 45M | **+0.0212 [+0.0140, +0.0282]** (0.8784 vs 0.8572) — CI-positive |
| confirm @ 46M (fresh) | **+0.0215 [+0.0157, +0.0273]** (0.8738 vs 0.8523) — replicated |
| item rate per opportunity (60 games @ 47M) | e28c **0.226 / 0.194** vs e28p 0.142 — **first item-behavior movement in the program** |
| boardkeep guard-rail (2000 mirrored @ 5M CRN) | 0.303 / 0.279 — in the e28p band, no hole |

Readings:

1. **Effect visibility converts to win rate** (+0.021 replicated) **and to
   the mechanism** (items up ~1.5x per opportunity) — even at the weak
   training dose (item-light zoo decks; the ~7k green-item hand appearances
   were evidently enough to teach the columns' meaning, generalizing to the
   item-rich ldraft deploy decks). E27's "consequence valuation" residual
   was partly plain feature incompleteness.
2. Sub-headroom (+0.021 < +0.03): promotion decision follows the E28
   pattern — train s2, then the stack bench (`lppo:e28c trio` vs the 0.908
   guarded RoR) decides both rungs. E7 precedent exists for promoting on
   zero-excluding CI + fresh replication.
3. The dose argument now points UP: fx at an item-light diet is already
   CI-positive; the fx + item-rich-training-decks arm (draft_override,
   E19 machinery) is the natural escalation.

Artifacts: runs/netprobe/e28c_bench_summary.json, e28c_fx_bc_summary.json.

## 2026-07-19 — E29 pre-read: fuse-layer loss is STRUCTURAL compression, not learned discarding

**Question.** The held-out probes confirm the fuse layer
(`features_extractor.head`, Linear 1344->256 + ReLU) is the largest single
step-drop in teacher-action decodability (netdmcts set: slots 0.575-0.582 ->
features 0.531-0.556, raw 0.607; E27: can_item 0.93 -> 0.76 at the same
step). Before designing the E29 arm: is the drop the 256-width bottleneck
itself, or did PPO TRAIN the fuse to discard tactical detail? Opposite
implications — a structural loss is fixed by a bypass (skip path / wider
`features_dim`) and is untouchable by conditioning the fuse; learned
discarding would put fuse-level objective work on the table.

**Method.** `scripts/e29_fuse_decomp.py`: teacher-action ridge probes
(masked argmax, game-level split — the net-probe protocol) on the fuse's
EXACT input `[slots ; scalar_mlp(scalars)]` (1344-d), the trained 256-d
output, and a random-init Linear+ReLU of the same shape applied to the same
input (3 replicates). b0k s0/s1/s2 on `runs/practicum-dmcts.npz` (28,035
states, dmcts teacher). Artifacts: `runs/netprobe/b0k_s*__fuse-decomp.json`.

| arm | s0 | s1 | s2 |
|---|---|---|---|
| fuse input (1344) | 0.386 | 0.385 | 0.379 |
| trained fuse (256) | 0.349 | 0.350 | 0.336 |
| random fuse (256, 3-seed range) | 0.341-0.354 | 0.341-0.345 | 0.343-0.355 |

**Read.** The trained fuse retains NO MORE action information than a random
projection of the same shape (s2 is even slightly below its random band) —
replicated 3/3 seeds. The loss is pure width compression + ReLU; PPO applies
zero pressure to preserve linear action-decodability at that layer, but does
not adversarially destroy it either. For E29 this kills "recondition/retrain
the fuse" as a standalone lever and supports the bypass family: E28's
pointer head IS that bypass for the action head (promoted); what still flows
ONLY through the fuse is the value-tower input and the pointer head's
tower context — a scalars/slot-pool skip into the towers (value tower
first, per E27's finding that the critic genuinely computes) and/or a wider
`features_dim` are the mechanically supported variants. Standing E27/E28
caveat: restored decodability moved broad decision quality, not items —
size expectations off gate-1's +0.04.

Caveats: linear-probe readout (towers may read nonlinearly); on-box
practicum data (greedy-draft states), not the held-out set — the held-out
drop is larger (0.58 -> 0.53) but same shape, and the decomposition should
transfer; replicate on the fast box if E29 hinges on the exact magnitude.

Data caveat discovered en route (affects ALL current probe practicums):
blue items have ZERO exposure in every practicum on hand — the mcts/dmcts
teachers draft with `GreedyDraftPolicy`, whose score `atk + def + 0.5*kw - 1`
is negative for every blue item (effects live in negative defense or in
php/ehp/draw fields the CardView does not expose), so no blue card was ever
drafted (0 in-hand states of 63k+ across practicum-flat/-token/-dmcts; red
items near-zero at ~370). Engine handling of blue items was audited against
the spec and is correct (`_apply_item`/`battle_legal`, sem action space) —
the blindness is draft-side only. Item-behavior probes on these datasets are
blind to blue (and nearly to red); for coverage, record with a draft
override (e.g. negative `item_discount` via the 5th spec param) rather than
changing greedy, which is pinned for reproducibility. Follow-up plan filed
as **E31** (docs/e31-draft-valuation-plan.md): E31a per-card value table
(data-only), E31b CardView hidden fields + spell-aware heuristic variant,
E31c draft-obs extension + ldraft retrain gated on E30 — correctness and
coverage scope, not strength (E17).

## 2026-07-19 — E28c stack: PROMOTED on both rungs — token-fx is the new recipe of record (0.878 / 0.914)

e28c_s2 trained (same single-trainer recipe), completing the 3-seed
artifact; scripts/e28c_stack_bench.py then mirrored the E28 promotion
ladder stage-for-stage with e28c in the candidate seat and the promoted
e28p artifact as the incumbent (fresh anchors 48M/49M; 43-47M were spent
by the e28p ladder and the e28c pair bench; guard seed 5M CRN kept
identical for band comparability):

| stage | result |
|---|---|
| pure trio vs e28p trio, 40x25 @ 48M | **+0.0170 [+0.0108, +0.0233]** (0.8784 vs 0.8614) |
| **stack: `lppo:e28c trio` vs guarded RoR (`lppo:e28p trio`) @ 48M** | **+0.0124 [+0.0089, +0.0159]** (0.9141 vs 0.9016) |
| stack confirm @ 49M (fresh) | **+0.0128 [+0.0079, +0.0177]** (0.9164 vs 0.9036) |
| lens increment ON fx nets @ 48M | +0.0352 [+0.0311, +0.0393] — headroom, mechanisms still disjoint |
| boardkeep vs the stack (2000 mirrored @ 5M CRN) | 0.2185 — matches e28p's 0.221, no hole |
| boardkeep vs pure s2 (same CRN) | 0.2525 — completes the 0.303/0.279 per-seed set |

Every promotion-relevant CI excludes zero, with a fresh-anchor
replication — the E7 precedent (sub-headroom, zero-excluding, replicated)
now satisfied three times over across the pair bench and the stack
ladder. **Promoted on both rungs**: reactive record 0.878, guarded record
**0.914** — a new summit, and the gap to the 0.926 ensemble planner RoR
is down to ~0.012.

Notes:

1. The lens increment (+0.035) is fx-invariant (+0.042 on e28p): effect
   visibility and the lethal-guard/ensemble mechanisms remain additive.
2. `lppo:` needed no changes for mixed-width models — the ensemble
   encodes per member via `ppo._encode_for`, which detects the fx variant
   from each model's observation space.
3. Published as `depot:e28c` v1 (parent e28p@1). The e28p recipes stand
   as the prior records in baseline.md.

Next (queued, pending go): the escalation arm — fx + item-rich training
decks via draft_override (E19 machinery). Dose logic points up: fx was
CI-positive on an item-starved diet; decks that actually contain the
cards it can now see are the obvious next lever.

Artifacts: runs/e28c-stack-summary.json, runs/e28c-stack-bench.log.

## 2026-07-20 — E29 slim re-probe: dropping the transformer RECOVERS the can_item signal at the tower input (+0.119, 3/3 seeds) — a mechanism for the slim win, and items are confirmed CHOICE not access

Post-merge diagnostic on the promoted `depot:e29slim` (PR #93). The E27
prestudy found `can_item` (an item play is legal) collapsing down the trunk —
0.98 raw-decodable, ~0.61 by the towers — one of the two named item failures.
Question: did the slim simplification (drop TokenSetExtractor's transformer for
SlimTokenExtractor's pooled context) change that representation, or just the
win rate?

Method: recorded a token-fx concept practicum at the EXACT params of the b0k
E27 run (`mcts:100`, 150 games, seed 1,000,000, node_cap 3000 → 33,186
labeled states), so the trajectories are IDENTICAL to the original — only the
encoding width differs (20 vs 17). This needed a small plumbing fix: the E27
recorder + `load_practicum` only knew `token` (17-wide); feeding that to an fx
net shape-mismatches the extractor. Added `token-fx` (variant="fx", 20-wide)
to `record_practicum`, `_Collector`, and the `load_practicum` layout gate
(scripts/e27_concept_probe.py `--obs-mode`). Probed e28c_s{0,1,2} and
e29slim_s{0,1,2} on the SAME data (paired), seed-averaged by
scripts/e29_slim_reprobe_summary.py.

Probe AUC, mean over 3 seeds (`features` = extractor output = tower input;
min_tower = worst of pi/vf_a{1,2}):

| concept | net | raw | features | min_tower | raw→min drop |
|---|---|---|---|---|---|
| **can_item** | e28c | 0.983 | 0.799 | 0.625 | +0.358 |
| **can_item** | **e29slim** | 0.983 | **0.918** | **0.657** | **+0.327** |
| item_now | e28c | 0.664 | 0.602 | 0.561 | +0.104 |
| item_now | e29slim | 0.664 | 0.660 | 0.535 | +0.129 |
| winner_side | e28c | 0.884 | 0.916 | 0.898 | −0.014 |
| winner_side | e29slim | 0.884 | 0.902 | 0.877 | +0.007 |
| lethal_now | e28c | 0.986 | 0.985 | 0.962 | +0.024 |
| lethal_now | e29slim | 0.986 | 0.987 | 0.956 | +0.030 |

**Finding 1 — a MECHANISM for the slim win.** `can_item` at the tower input
jumps e28c 0.799 → slim **0.918** (+0.119), and the trend is monotone with
win rate across the whole line: b0k 0.761 (s0) → e28c 0.799 → slim 0.918. The
transformer was destroying item-presence information BEFORE it reached the
towers; the slim pooled context preserves it (worst-case tower retention also
better, 0.657 vs 0.625). This is the E28b/encoder-viz story cashed out at the
representation level — the dropped attention was overfitting capacity that
degraded a signal the raw obs carries at 0.98. Replicated 3/3 seeds.

**Finding 2 — items are CHOICE, not access (confirms E31c targeting).**
`item_now` (does the teacher play an item HERE, on item-legal states) is weak
in ALL nets — ~0.66 raw, barely predictive — and slim is no better down the
towers. So the net now retains WHICH items are playable but still does not
linearly encode WHICH one a lookahead teacher picks. That is the
consequence-valuation / card-design residual (E30 closed representational;
E31c gated on the card pool), NOT a representation-access problem an arch arm
could fix. No new trunk arm indicated.

Controls: `winner_side` — slim's value tower runs a hair lower (0.922 vs
0.932 at vf_a1; min_tower 0.877 vs 0.898), the small, replicated price slim
pays for dropping capacity in the one place the trunk genuinely computes; it
does not hurt play (slim 0.903 > e28c 0.878). `lethal_now` near-ceiling
everywhere (towers ~0.96), lens-guarded regardless.

Caveats: linear-probe decodability is necessary-not-sufficient for "uses";
single concept practicum (one teacher, one seed of states). Plumbing (token-fx
recording + load gate) is a clean reusable addition with tests
(test_distill_token_load fx round-trip + v0-width rejection).

Artifacts: runs/netprobe/e29_reprobe_summary.json,
runs/netprobe/e27_{e28c,e29slim}_s{0,1,2}.json. Tools:
scripts/e29_slim_reprobe_summary.py, scripts/e29_slim_reprobe_compare.py.


## 2026-07-20 — E32 search re-baseline (Phase 0+1): the evaluator swap to depot:e29slim is NULL-to-NEGATIVE — a stronger REACTIVE net is a WORSE search evaluator, and the value/policy-head split predicts which

Follow-up to the E29 slim promotion (#93): the play-time-search recipes of
record (`rbeam`, `netdmcts`) use the `shared` critic ensemble as their
evaluator net. `depot:e29slim` is a stronger REACTIVE net (0.903 pure) — does
swapping it in as the search evaluator lift the search ceiling? The E29 slim
re-probe made this genuinely uncertain: e29slim was bred for the pointer/policy
head, and its VALUE tower is marginally weaker (winner_side 0.922 vs 0.932),
while search leans on the value head.

**Pre-registered prediction:** `netdmcts` (PUCT policy-prior driven → uses
e29slim's STRONGER pointer head) benefits ≥ `rbeam`/`vbeam` (value-driven → the
weaker value tower).

**Phase 0 — feasibility + cost** (scripts/e32_phase0_smoke.py; e29slim trio vs
shared trio, matched config, vs greedy). All three wrappers RAN with the
e29slim evaluator — incl. netdmcts's pointer-head PUCT-prior path, the one path
not previously exercised on a pointer/slim net — and all beat greedy (sanity
1.0). Cost ratio e29slim/shared FELL with eval density: vbeam(8,20) 1.93x,
rbeam(8,20,4,4) 1.26x, netdmcts(1,320) **0.84x**. So at the deep RoR configs
e29slim is ~cost-parity (netdmcts slightly cheaper) — the "7.4x cheaper → free
depth" framing was wrong (extractor cut is only ~1.17x end-to-end, and the
baseline `shared` is already a lean non-pointer critic). Phase 2 (cost-freed
depth) DROPPED — no freed budget.

**Phase 1 — evaluator swap** (scripts/e32_search_rebaseline.py; head-to-head,
matched config, matched `ldraft` both sides so the evaluator net is the ONLY
difference; run_match mirrors seats; seed base 30M; 100 seed pairs = 200 games
each; block-resumable under runs/e32-blocks/):

| arm | mechanism | e29slim-eval WR vs shared-eval | Wilson CI (n=200) | verdict |
|---|---|---|---|---|
| **rbeam** (8,20,4,4) | value-driven | **0.335** | [0.273, 0.403] | **BEHIND** (CI < 0.5) |
| **netdmcts** (1,320,1.5) | PUCT policy-prior | **0.440** | [0.373, 0.509] | **TIE** (straddles 0.5) |

**Verdict: swap CLOSED, null-to-negative — `shared` stays the search evaluator
of record, no re-pin.** Swapping the reactive champion into the evaluator role
does not help, and for `rbeam` it significantly HURTS. **The prediction held
cleanly: netdmcts (0.440) ≫ rbeam (0.335)**, and the ordering is
mechanism-consistent — rbeam leans on the weaker value tower and is penalized;
netdmcts leans on the stronger pointer priors and only ties. This is the E7
thesis ("policy vs value heads want different data") confirmed at the search
level: **a net bred for reactive play is not automatically a better critic for
search.** Not a bug — e29slim-search crushed greedy 1.0 in Phase 0, the mirror
is balanced (rbeam seat0 0.40/seat1 0.27; netdmcts seat0 0.52/seat1 0.36), and
the asymmetry is mechanism-consistent.

No confirm run: `rbeam` is decisively negative and `netdmcts` is at best
parity, so neither can become a positive that re-pins the RoR — a 500-game
tighten changes no decision (and both-sides-search games run long, ~85 s/game
netdmcts). **Opens (if search is ever pursued again):** a value-head-finetuned
e29slim variant as a DEDICATED search evaluator — the swap negative localizes
exactly to the value head, so that is the motivated arm, not the reactive net
as-is.

Next (queued): Phase 3 — the program question, using the ACTUAL search RoR
(`rbeam:shared`, since e29slim is not a better evaluator) vs the `lppo:e29slim`
guarded stack (0.934) head-to-head. With the reactive stack now above the 0.926
planner, does fair play-time search STILL beat the reactive rung, or has the
reactive net caught it? Cheap (~7 s/game: one-sided search + the light lppo).

Artifacts: runs/e32-search-rebaseline.json, runs/e32-blocks/ (gitignored like
the E24 blocks). Tools: scripts/e32_search_rebaseline.py (Phase 1 driver,
block-resumable), scripts/e32_phase0_smoke.py (feasibility + cost gate).

### E32 Phase 3 — does fair search STILL beat the reactive rung? YES, decisively — the reactive net has NOT caught up (0.788 head-to-head)

The program's terminal question. Fair-search RoR `rbeam:shared` (evaluator stays
shared per Phase 1) vs the reactive rungs, head-to-head, matched `ldraft` both
sides, run_match mirrored, n=500 each, seed 30M (same e32 driver, `--arm phase3`):

| candidate (fair search) | vs reactive baseline | search WR | Wilson CI (n=500) | verdict |
|---|---|---|---|---|
| `rbeam:shared` 8,20,4,4 | `lppo:e29slim` guarded stack (0.934) | **0.788** | [0.750, 0.822] | search AHEAD |
| `rbeam:shared` 8,20,4,4 | `ppo:e29slim` pure trio | 0.808 | [0.771, 0.840] | search AHEAD |

**Verdict: fair play-time search STILL decisively beats the reactive rung —
0.788 [0.750,0.822] vs the 0.934 guarded stack (search wins ~79-21). The
reactive net has NOT caught fair search.** The lens guard buys only ~0.02 here
(0.808 -> 0.788 against) — against a SEARCHING opponent a shallow tactical guard
barely helps; what's missing is lookahead, which the guard does not supply.
Balanced seats (guarded 0.828/0.748, pure 0.836/0.780), ~11.7 s/game.

**Resolves the ruler paradox.** Milestone 3 said the guarded stack (0.934)
SURPASSED the 0.926 ensemble planner, yet here it LOSES to fair search 21-79.
Both true: the avg-hard3 POOL ruler saturates near the top (strong policies all
~0.93 — the E25 strong-league already flagged "the HARD3 pool saturates so it
can't discriminate"), so 0.934>0.926 means the reactive net matches the planner
vs WEAK opponents, NOT that it caught adversarial search. The head-to-head
ruler is unsaturated, and there lookahead search dominates. The reactive-limits
gap to fair search is WIDE OPEN on the discriminating ruler (~0.29 above parity).

**Program conclusion.** E28/E29 reactive gains were real and large ON THE POOL
but did NOT close the head-to-head gap to lookahead search — exactly the program
doc's prediction ("the remaining gap is lookahead depth, which play-time search
supplies"), now quantified at 0.788. Both trunk levers that could add lookahead
are closed (E29a conditioning, E30 turn-plan factorization), so the reactive net
cannot catch fair search by trunk/training work alone. Play-time search remains
the only source of lookahead — and per Phase 1 it should keep using `shared`,
not the reactive champion, as its evaluator.

Artifacts: runs/e32-phase3.json, runs/e32-blocks/confirm/ (gitignored). Same
driver, `--arm phase3 --stage confirm`.

## 2026-07-20 — E33 behavioral autopsy: fair search's edge over the reactive net is BOARD CONTROL (trading), not tactics or resources — a policy-vs-value mismatch inside the net

E32 Phase 3 showed fair search beats the reactive rung 0.788 head-to-head but
only quantified the gap. E33 maps its SHAPE. Subject drives 120 games vs the
HARD3 pool (scripts/e33_reactive_vs_search_behavior.py); at each subject
turn-start we shadow, from the identical state, (a) the ORACLE turn plan =
`vbeam` over the SAME e29slim trio — one turn of pure lookahead on the very net
the subject uses, so any difference is SEARCH not a different net — and (b) the
LETHAL oracle = `lguard.find_lethal` (exhaustive DFS, unbiased ground truth).
Two subjects: `lppo:e29slim` (guarded RoR) and `ppo:e29slim` (pure), ~900
decision-turns each.

| per-turn metric | lppo (guard) | ppo (pure) | lookahead plan |
|---|---|---|---|
| **missed lethal rate** | **0.000** (0/115) | **0.102** (13/127) | — |
| attacks to face | 1.15 | 1.17 | **0.942** |
| attacks to trade | 0.703 | 0.715 | **0.910** |
| face-share of attacks | 0.582 | 0.589 | **0.482** |
| items played | 0.181 | 0.168 | **0.226** |
| mana unspent | 0.423 | 0.446 | 0.417 |
| first-action disagreement | 0.357 | 0.377 | — |

**Three findings.**
1. **NOT tactics.** The lethal guard drives missed forced-wins 10.2% -> 0
   (matches the old ~18% missed-lethal on b0k, lower on the fx net). But the
   guarded stack still loses to search 0.788 — so tactics is not the gap.
2. **NOT resources.** Mana unspent is identical to lookahead (0.42 vs 0.42);
   playable cards almost always get played. The curve is not fumbled.
3. **IS board control.** Same NUMBER of attacks, pointed differently: reactive
   goes **+22% to face, -23% to trades** (face-share 0.58 vs 0.48), and plays
   **~18% fewer items**. Lookahead sacrifices immediate hero damage to kill
   enemy minions; the reactive net grabs the tempo. Divergence is FRONT-LOADED
   (biggest first-divergence bucket is index 0 — the turn's opening move differs
   ~1/3 of turns; same_end ~0.30 is order noise).

**Guard signature is ORTHOGONAL to the positional signature.** Guarded and pure
are identical on face/trade/items/ordering and differ ONLY on missed lethals —
the lethal guard is a clean tactical patch that leaves positional judgment
untouched, which is exactly the residual gap.

**The deep read: a policy-vs-value mismatch INSIDE one net.** The oracle is this
net's own VALUE head + one ply of lookahead; the reactive choice is the same
net's POLICY head. So the face-greed is an internal disagreement — the value
function (under lookahead) prefers trading for board control, but the policy head
is face-greedy. One ply surfaces a preference the bare policy head won't act on.
This is the "depth beats info" / "starts lines it can't finish" story (E22/E30)
behaviorally localized, and it dovetails with the CLOSED training-side absorption
program (E9/E15 imitation): the policy head never internalized the planner's
trade-for-board-control preference. Mechanistically the 0.788 gap IS the
multi-turn value of board control a zero-lookahead policy can't see.

Caveat: the oracle is 1-ply `vbeam` on the same net — the WEAKEST lookahead, and
a LOWER BOUND on the divergence; the deeper/stronger `rbeam:shared` (Phase 3
champion) would diverge more. States are from HARD3-pool games (cheap,
representative of the subject's own play); head-to-head-vs-rbeam states could
shift the distribution. Linear-behavioral, not causal.

Artifacts: runs/e33-lppo.json, runs/e33-ppo.json, runs/e33-*-raw.jsonl.gz
(gitignored). Tool: scripts/e33_reactive_vs_search_behavior.py (parameterizable
subject; per-turn shadow driver reusing the E14a pattern + lguard oracle).
Follow-up levers this SELECTS: (1) a board-control / trade-value auxiliary or
reward-shaping term on the policy head; (2) re-run with `rbeam:shared` oracle to
size the full gap; (3) card-id-level item-usage breakdown (which items get
under-played).

